---
id: 202506-宁波九校-高一-期末-15
题型: 填空题
来源: "[[试卷/202506-宁波九校-高一-期末.pdf|202506-宁波九校-高一-期末.pdf]]"
试卷: 202506-宁波九校-高一-期末.pdf
年级: 高一
题号: "15"
难度: 困难
水平: 选考
知识点:
  - 03python基础
  - 08数组
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-22
图片核验: 无图片
tags:
  - 知识点/03python基础
  - 知识点/08数组
---
# 题目 15

15. 现有一组取值在 0~127 之间的整数数据，采用混合加密算法完成加密。设定分段长度为 `m`，将所有数据每 `m` 个分为一段，最后不足 `m` 个自成一段。加密流程如下：

①数值加密：设数值密钥为列表 `a`，长度为 `m`，对分段内 `i` 位置的数据运算：加密值 = `(原数据 + a[i]) % 128`。  
②位置加密：设位置密钥为列表 `b`，长度为 `m`，元素为 1~m 不重复整数，加密的 `i` 位置数据会被存放于 `(b[i]-1)` 位置。  
③其中满 `m` 个的数据分段会执行数值加密和位置加密，不足 `m` 个的数据分段只做数值加密，不做位置加密，分段加密完成后再按顺序存放形成加密数据段。

例如：设分段长度 `m=3`，数值密钥 `a=[2,5,7]`，位置密钥 `b=[2,3,1]`，分段数据 `[10,20,30]`，最终加密后数据段为 `[37,12,25]`。

（1）已知分段长度 `m=4`，数值密钥 `a=[5,12,8,3]`，位置密钥 `b=[2,4,1,3]`，分段数据 `[20,66,90,15]`，该段数据加密后的结果为______。

（2）完善程序代码，实现数据加密功能。

```python
def new(c, t, a):
    lst = []
    for i in range(t):
        newd = (c[i] + a[i]) % 128
        lst += [newd]
    return lst

# 获取原始数据放在 data 中，代码略
# 获取数值密钥 a、位置密钥 b、分段长度 m，代码略
n = len(data)
encode = []
for i in range(0, n, m):
    ______①______
    length = len(seg)
    ______②______
    if length == m:
        form = [0] * m
        for k in range(m):
            ______③______
            form[pos] = trans[k]
        encode += form
    else:
        ______④______
print("原始数据：", data)
print("加密后数据：", encode)
```

---

## 答案

**正确答案：** （1）`[98,25,18,78]`；（2）① `seg = data[i:i+m]`；② `trans = new(seg, length, a)`；③ `pos = b[k] - 1`；④ `encode += trans`

---

## 解析

依据题干中的关键概念、数据表示、系统组成或程序执行过程判断，正确答案与题意一致。

---

## 相关链接

- 相关知识点：
