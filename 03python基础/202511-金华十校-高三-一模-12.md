---
id: 202511-金华十校-高三-一模-12
题型: 选择题
来源: "[[试卷/202511-金华十校-高三-一模.pdf|202511-金华十校-高三-一模.pdf]]"
试卷: 202511-金华十校-高三-一模.pdf
年级: 高三
题号: "12"
难度: 中等
水平: 选考
知识点:
  - 03python基础
  - 字符串处理
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-04
图片核验: 无图片
tags:
  - 知识点/03python基础
---

# 题目 12

12. 编写代码，实现功能：消除字符串 `s` 中连续三个及以上相同的字母，如果消除后出现新的连续字符则一并消除。例如输入 `"abbbbc"`，输出结果为 `"ac"`。

```python
s = input() + "."
s1 = [""] * (len(s) + 1); t1 = 0    # 存储结果字符
s2 = [0] * (len(s) + 1); t2 = 0     # 存储连续计数
for i in s:
    while s2[t2] >= 3 and i != s1[t1]:
        t1 -= s2[t2]; t2 -= 1
    if i == s1[t1]:
        ①
    else:
        t2 += 1; s2[t2] = 1
        t1 += 1; ②
# 输出 s1[1:t1] 的内容，代码略
```

则划线处应填的代码是（   ）

- A. ① `s2[t2] += 1` ② `s1[t1] = s1[s2[t2]]`
- B. ① `s1[t1] = i` ② `s1[t1] = s1[t1-1]`
- C. ① `s2[t2] += 1` ② `s1[t1] = i`
- D. ① `s1[t1] = i` ② `s1[t2] = 1`

---

## 答案

**正确答案：** C

---

## 解析

`s1` 模拟栈存储结果字符，`s2` 模拟栈存储对应字符的连续计数。

① 当 `i == s1[t1]`（当前字符与栈顶字符相同），将连续计数加 1：`s2[t2] += 1`。
② 当 `i != s1[t1]`（遇到新字符），先推进 `t2` 并初始化计数为 1，再推进 `t1` 并将新字符存入：`s1[t1] = i`。

当 `s2[t2] >= 3` 且下一个字符不同时，需要回退栈（消除这三个及以上连续字符）。

因此 ① 选 `s2[t2] += 1`，② 选 `s1[t1] = i`，选 C。

---

## 相关链接

- 相关知识点：栈模拟、字符串消除
