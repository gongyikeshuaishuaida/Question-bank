---
id: 202605-强基联盟-高三-月考-15
题型: 填空题
来源: "[[试卷/202605-强基联盟-高三-月考.pdf|202605-强基联盟-高三-月考.pdf]]"
试卷: 202605-强基联盟-高三-月考.pdf
年级: 高三
题号: "15"
难度: 困难
知识点:
  - 03python基础
完成次数: 0
正确率:
状态: 未练习
错题原因:
创建日期: 2026-06-23
图片核验: 已通过
tags:
  - 状态/未练习
  - 知识点/03python基础
---

# 题目 15

15. 集装箱按体积从小到大分为 6 种规格，依次标记为 `A-F`。所有集装箱按预约时间依次到港，可竖直叠放成堆，但必须满足：后到集装箱不能放在先到集装箱的下方；体积小的集装箱不能放在体积大的集装箱下方。

为满足叠放要求，采用以下策略：新箱到港时，查找堆顶集装箱体积大于或等于新箱体积的堆。若有多个，选顶部集装箱体积最小的堆叠放；若无，则新建一堆放置。如，集装箱到港顺序为：DCBBCAABC，可以将箱子分为 `[D, C, B, B, A, A]`、`[C, B]`、`[C]` 三堆。设计 Python 程序，求解集装箱最少码放堆数。

（1）如果集装箱进港顺序为 CCDBACACDBA，箱子堆数至少为 ______。

（2）不同时间抵达港口的集装箱，按到达时间先后依次处理。为便于后续堆放，同一时间到达的集装箱中，体积较大的先进行堆放。定义如下 `sort_d(data)` 函数，用于预处理数据，`data` 列表的每个元素含 2 个数据项，依次为到达时间、规格类型，如第 15 题表所示。

![[attachments/202605强基联盟高三月考_15_图1.png]]

```python
def sort_d(data):
    n = len(data)
    for i in range(1, n):
        key = data[i]
        j = i - 1
        while j >= 0:
            if data[j][0] > key[0] or (data[j][0] == key[0] and data[j][1] < key[1]):
                data[j + 1] = data[j]
                j -= 1
            else:
                break
        data[j + 1] = key
    s = ""
    for i in range(n):
        s = s + data[i][1]
    return s
```
① 加框处代码的运行次数为 ______。

② 在调用函数 `sort_d(data)` 后，返回值为 ______。

（3）实现堆叠功能的部分程序如下：
若 `data` 中数据为第 15 题表中数据，则输出结果如第 15 题图所示。请在划线处填入合适的代码。

![[attachments/202605强基联盟高三月考_15_图2.png]]

```python
def stacking(box):
    n = len(box)
    top = []      # 每个堆最上面的集装箱规格
    top_idx = []
    prev = [-1] * n
    for i in range(n):
        cur = ord(box[i]) - 65
        left, right = 0, len(top)
        _____①_____
        while left < right:
            mid = (left + right) // 2
            if _____②_____:
                pos = mid
                right = mid
            else:
                left = mid + 1
        if pos < len(top):
            prev[i] = top_idx[pos]
            _____③_____
            top_idx[pos] = i
        else:
            top.append(cur)
            top_idx.append(i)
    print("具体堆放方案：")
    for i in range(len(top)):
        pile = []
        curr_idx = top_idx[i]  # 从堆顶开始
        while curr_idx != -1:
            pile.append(box[curr_idx])
            curr_idx = prev[curr_idx]
        # 将 pile 列表倒转输出，格式如第 15 题图，代码略
    return len(top)

"""
将集装箱到港数据存入 data 列表中，data 列表的每个元素含 2 个数据项，依次为到达时间、规格类型，代码略。
"""
d_str = sort_d(data)
print(stacking(d_str))
```

---

## 答案

**正确答案：** （1）3；（2）① 6；② `DBAACCB`；（3）① `pos = right`；② `top[mid] >= cur`；③ `top[pos] = cur`

---

## 解析

按堆顶不小于新箱且尽量小的策略模拟 `CCDBACACDBA`，最少 3 堆。第 15 题表排序时同一时间规格大的先排，插入排序移动语句执行 6 次，得到 `DBAACCB`；二分查找默认 `pos=right`，寻找首个 `top[mid] >= cur` 的堆并用 `top[pos]=cur` 更新堆顶。

---

## 相关链接

- 来源：[[试卷/202605-强基联盟-高三-月考.pdf|202605-强基联盟-高三-月考.pdf]]
