---
id: 202606-慈溪-高二-期末-13
题型: 填空题
来源: "[[试卷/202606-慈溪-高二-期末.pdf|202606-慈溪-高二-期末.pdf]]"
试卷: 202606-慈溪-高二-期末.pdf
年级: 高二
题号: "13"
难度: 困难
水平: 选考
知识点:
  - 03python基础
  - 08数组
  - 前缀和
  - 字典
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-28
图片核验: 无图片
tags:
  - 知识点/03python基础
  - 知识点/08数组
  - 知识点/前缀和
  - 知识点/字典
---

# 题目 13

13. 某线上商店为了促进消费，推出了一项免单活动：从一天内的所有订单中寻找连续若干笔订单的累计消费总金额恰好为 `s` 元的订单段，若只有一个符合要求的订单段，则该订单段中所有订单免单；若有多个符合要求的订单段，则选择其中最长的订单段（若最长的长度相等，则选择先出现的）并对其中所有订单免单。若某天订单数据：

```python
orders = [
    ["X001", 3], ["X002", 8], ["X003", 2], ["X004", 1], ["X005", 7],
    ["X006", 1], ["X007", 2], ["X008", 5], ["X009", 3], ["X010", 3]
]
```

`orders` 中每个元素包含两个数据项，第一个数据项表示订单号，第二个数据项表示该笔订单的消费金额。若 `s` 的值为 10，则免单的订单号为 X003，X004，X005。编写程序，根据一天的订单数据，输出免单的订单号。

(1) 按上述 `orders` 的值，若 `s` 的值为 11，则免单的订单号为________（订单号间用逗号隔开）。

(2) 实现上述功能的部分 Python 程序如下，请在划线处填入合适的代码。

```python
# 获取当天订单数据 orders，代码略，格式如题中所述
s = int(input("请输入消费总金额s:"))
pos = {0: -1}
start = end = -1
mx = 0
____①____
for i in range(len(orders)):
    ze = ze + orders[i][1]
    if ze not in pos:
        pos[ze] = i  # orders[0]到orders[i]订单累计消费总金额为ze
    ____②____
    if tg in pos:
        st = pos[tg] + 1
        if ____③____:  # 寻找最长的订单段
            mx = i - st + 1
            start = st
            end = i
if start == -1:
    print("没有符合条件的订单段！")
else:
    print("免单的订单号为：")
    for i in range(start, end + 1):
        print(orders[i][0], end="，")
```

---

## 答案

(1) X003，X004，X005，X006

(2) ① `ze=0`；② `tg=ze-s`；③ `i-st+1>mx`（或 `i-pos[tg]>mx`）

---

## 解析

(1) X003 至 X006 的消费金额之和为 `2+1+7+1=11`，对应最长的符合条件订单段。

(2) `ze` 保存从第 0 笔到当前订单的前缀和，初值应为 0。若当前前缀和为 `ze`，要使某段和为 `s`，需查找此前是否出现前缀和 `ze-s`，所以令 `tg=ze-s`。`pos` 只保存某个前缀和第一次出现的位置，因此在 `i-st+1>mx` 时更新，既能保留最长订单段，也能在长度相同时保留先出现的订单段。

---

## 相关链接

- 相关知识点：
