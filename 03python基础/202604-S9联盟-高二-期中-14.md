---
id: 202604-S9联盟-高二-期中-14
题型: 填空题
来源: "[[试卷/202604-S9联盟-高二-期中.pdf|202604-S9联盟-高二-期中.pdf]]"
试卷: 202604-S9联盟-高二-期中.pdf
年级: 高二
题号: "14"
难度: 中等
水平: 选考
知识点:
  - 03python基础
  - 08数组
  - 双指针
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-30
图片核验: 无图片
tags:
  - 知识点/03python基础
  - 知识点/08数组
  - 知识点/双指针
---

# 题目 14

接上题，将这一天的所有温度数据（每分钟只有一条数据）存储于列表 `data` 中（`data` 形如 `[[采集时间, 温度数据], …]`），要求找出温度连续下降的最长连续序列，如果这样的序列有多个，选择最早出现的。请回答下列问题：

（1）`data=[[0, 37.9], [1, 37.7], [2, 37.7], [3, 37.6], [4, 37.7], [5, 37.5]]`，则温度连续下降的最长连续序列的开始索引为 ________。

（2）实现上述功能的部分 Python 程序如下，请在划线处填入合适的代码。

```python
# 定义如下 proc(x) 函数，功能为根据正整数 x 值返回相应时间，
# 如 x 为 0，则返回"00:00"；如 x 为 480，则返回"08:00"，代码略
# 按采集的时间顺序存储于列表 data 中，形如[[采集时间, 温度数据], …]，代码略
n = len(data)
____①____
maxl = 1
pos = -1
while i < n:
    if data[i][1] < data[i-1][1]:
        j = i + 1
        while j < n and data[j][1] < data[j-1][1]:
            j += 1
        if ____②____:
            pos = i - 1
            maxl = j - i + 1
        ____③____
    i += 1
print("持续最长的温度连续下降时段为：")
timeA = proc(data[pos][0])
timeB = proc(data[pos+maxl-1][0])
print(timeA+"至"+timeB)
```

---

## 答案

（1）0

（2）① `i = 1`；② `j - i + 1 > maxl`；③ `i = j`（或 `i += 1`）

---

## 解析

（1）三个长度为 2 的严格下降段分别从索引 0、2、4 开始，最早的是索引 0。

（2）从索引 1 开始与前一项比较；`j-i+1` 是当前下降序列长度，只有严格大于已记录长度时才更新，以保留最早出现的最长序列；处理完一个下降段后移动 `i`，避免重复扫描。

---

## 相关链接

- 相关知识点：[[06信息系统/202604-S9联盟-高二-期中-13]]
