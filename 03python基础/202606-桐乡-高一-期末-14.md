---
id: 202606-桐乡-高一-期末-14
题型: 填空题
来源: "[[试卷/202606-桐乡-高一-期末.pdf|202606-桐乡-高一-期末.pdf]]"
试卷: 202606-桐乡-高一-期末.pdf
年级: 高一
题号: "14"
难度: 中等
水平: 选考
知识点:
  - 03python基础
  - 08数组
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-22
图片核验: 无图片
tags:
  - 知识点/03python基础
  - 知识点/08数组
---
# 题目 14

14.“数字进位”游戏规则如下：
①初始有n 个1，存储在一个列表中；
②从左到右扫描列表，找到两个相邻且数值相同的元素，将前一个元素值加1,并删除后
一个元素；
③继续扫描，直到不存在两个相邻且数值相同的元素为止；
④输出最后的结果。
例如5 个1 的进位合并过程：[1,1,1,1,1] →[2,1,1,1] →[2,2,1] →[3,1]

请回答下列问题：
（1）若n=7,输出的结果是
▲
（2）实现上述功能的Python 代码如下，请在划线处填入合适的代码
```python
n = int(input("请输入初始数量n："))
a = [1] * n
①
while i < len(a) - 1:
    if a[i] == a[i + 1]:
        ②
        a = a[:i + 1] + a[i + 2:]
        if i > 0:
            ③
    else:
        i = i + 1
print(a)
```
（3）实现数据还原的功能，将进位合并的结果还原成n 个1。例如：输入[3,1]，输出5
个1，实现该功能的Python 代码如下，请在划线处填入合适的代码
```python
def restore(a):
    # 数据还原
    n = 0
    for i in a:
        n = ______
    return n

print("一共有" + str(restore(a)) + "个1")
```

---

## 答案

**正确答案：** (1) `[3,2,1]`；(2) ① `i = 0`；② `a[i] = a[i] + 1` 或 `a[i] += 1`；③ `i = i - 1` 或 `i -= 1`；(3) `n + 2**(i - 1)`

---

## 解析

按题干规则逐步模拟变量变化，空位代码应与条件判断、循环更新和输出目标一致。

---

## 相关链接

- 相关知识点：
