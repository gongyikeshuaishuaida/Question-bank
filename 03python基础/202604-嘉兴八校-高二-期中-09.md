---
id: 202604-嘉兴八校-高二-期中-09
题型: 选择题
来源: "[[试卷/202604-嘉兴八校-高二-期中.pdf|202604-嘉兴八校-高二-期中.pdf]]"
试卷: 202604-嘉兴八校-高二-期中.pdf
年级: 高二
题号: "09"
难度: 简单
水平: 选考
知识点:
  - 03python基础
  - 分支结构
  - 逻辑运算
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-30
图片核验: 无图片
tags:
  - 知识点/03python基础
  - 知识点/分支结构
  - 知识点/逻辑运算
---

# 题目 09

学校要挑选身高在 1.65 米及以上（含 1.65 米）且 1.73 米以下（不含 1.73 米）的女生参加啦啦队，下列 Python 代码片段中，逻辑正确的是（   ）

- A.

  ```python
  if height <= 1.73:
      if height >= 1.65:
          print("入选")
      else:
          print("太矮")
  else:
      print("太高")
  ```

- B.

  ```python
  if height >= 1.65 or height < 1.73:
      print("入选")
  else:
      print("不入选")
  ```

- C.

  ```python
  if height >= 1.65:
      print("入选")
  if height < 1.73:
      print("入选")
  ```

- D.

  ```python
  if height >= 1.65 and height < 1.73:
      print("入选")
  else:
      print("不入选")
  ```

---

## 答案

**正确答案：** D

---

## 解析

入选条件是 `height>=1.65` 与 `height<1.73` 同时成立，应使用 `and` 连接。A 会把恰好 1.73 米判为入选；B 使用 `or` 导致范围过宽；C 的两个独立 `if` 不能保证同时满足两个边界条件。

---

## 相关链接

- 相关知识点：
