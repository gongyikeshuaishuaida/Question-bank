---
id: 202511-S9联盟-高二-期中-15
题型: 填空题
来源: "[[试卷/202511-S9联盟-高二-期中.pdf|202511-S9联盟-高二-期中.pdf]]"
试卷: 202511-S9联盟-高二-期中.pdf
年级: 高二
题号: "15"
难度: 中等
水平: 选考
知识点:
  - 03python基础
  - 字符串
  - 加密解密
  - 循环结构
  - 07信息安全
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-30
图片核验: 无图片
tags:
  - 知识点/03python基础
  - 知识点/字符串
  - 知识点/加密解密
  - 知识点/循环结构
  - 知识点/07信息安全
---

# 题目 15

数据通过公共信道传输，数据面临着被非授权读取、截获、篡改、伪造等一系列安全问题。为确保信息安全，信息需加密传输，发送方由明文→密文（加密），接收方由密文→明文（解密）。某字符加密算法的思路如下：

1）只针对英文字符和数字进行加密，其余字符保持不变；

2）将原文中的小写字母转换成大写字母；

3）根据第 2 步所得结果，若是英文字符，则逐个后移 4 位（例如“A”→“E”，“Z”→“D”）；若是数字，则逐个前移 2 位（例如“5”→“3”，“0”→“8”）；

4）将字符倒序输出，例如“abc”→“cba”。

请回答下列问题：

（1）若输入的明文是“BY8+油”，则得到的密文是 ________。

（2）用计算机解决问题的过程一般包括四步：①设计算法；②调试运行程序；③编写程序；④抽象与建模，这四个步骤的正确顺序是 ________。

（3）若输入 10 个字符依次对每个字符进行加密处理，则最适合采用的算法结构是 ________（单选，填字母）。

- A. 顺序结构
- B. 分支结构
- C. 循环结构

（4）编写如下 Python 程序段，实现对单个字符 `x` 的加密，请在划线处填入合适的代码。

```python
if "a" <= x <= "z":
    x = ________①
if "A" <= x <= "Z":
    ans = ________②
elif "0" <= x <= "9":
    ans = str((int(x) - 2) % 10)
else:
    ans = x
```

---

## 答案

（1）`油+6CF`

（2）④①③②

（3）C

（4）

- ① `chr(ord(x)-32)`
- ② `chr((ord(x)-ord("A")+4)%26+ord("A"))`（或 `chr((ord(x)-65+4)%26+65)`）

---

## 解析

（1）`B、Y` 分别后移 4 位得到 `F、C`，数字 8 前移 2 位得到 6，其他字符不变，得到 `FC6+油`；倒序后为 `油+6CF`。

（2）先抽象与建模，再设计算法、编写程序，最后调试运行，顺序为④①③②。

（3）需要对 10 个字符重复执行同类处理，适合采用循环结构。

（4）ASCII 编码中小写字母比对应大写字母大 32，因此减 32 可转成大写；字母后移 4 位时用取模保证越过 Z 后从 A 继续。

---

## 相关链接

- 相关知识点：
