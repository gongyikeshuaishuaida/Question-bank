---
id: 202604-嘉兴八校-高二-期中-13
题型: 填空题
来源: "[[试卷/202604-嘉兴八校-高二-期中.pdf|202604-嘉兴八校-高二-期中.pdf]]"
试卷: 202604-嘉兴八校-高二-期中.pdf
年级: 高二
题号: "13"
难度: 中等
水平: 选考
知识点:
  - 03python基础
  - 字符串
  - 加密解密
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-30
图片核验: 已通过
tags:
  - 知识点/03python基础
  - 知识点/字符串
  - 知识点/加密解密
---

# 题目 13

小明设计了一种字符加密方法，用原密码加密生成新密码。他设计的加密规则如下：

【1】将原密码中的小写字母转换成大写字母；  
【2】将原密码中的大写字母利用移位秘钥中对应的数字循环右移，移位秘钥不够时可以循环使用。如移位密钥为“312”，待加密字母“ABcDEF”，则应将字母 A、B、D、E、F 分别循环右移 3 位、1 位、2 位、3 位、1 位；  
【3】将原密码中的数字转换为与其对称的数字，例如：0→9……3→6……5→4……9→0；  
【4】原密码中其它特殊字符不改变。

程序运行界面如图所示，实现上述功能的程序代码段如下：

![[attachments/202604嘉兴八校高二_13_图1.png]]

```python
pw = input("请输入原密码：")
yw = input("请输入移位密钥：")
res = ""
n = 0
for ch in pw:
    if "a" <= ch <= "z":
        ch = ch.upper()  # 将小写字母转化为大写字母
    ____①____ "A" <= ch <= "Z":
        ch = chr((ord(ch) - 65 + int(yw[n])) % 26 + 65)
        n = ____②____
    elif "0" <= ch <= "9":
        ch = str(____③____)
    res = res + ch
print("生成新密码：", res)
```

（1）①处的代码应填 ________（单选，填字母：A. `elif` / B. `if`）。

（2）②处的代码应填 ________（单选，填字母：A. `(n+1)/len(yw)` / B. `(n+1)//len(yw)` / C. `(n+1)%len(yw)`）。

（3）请在划线处填上合适代码。③ ________。

---

## 答案

（1）A  
（2）C  
（3）`9-int(ch)`（或 `57-ord(ch)`）

---

## 解析

（1）原密码中的小写字母只转换为大写，不再执行移位，因此第二个条件必须与第一个 `if` 互斥，应填 `elif`。  
（2）每处理一个原大写字母，密钥下标向后移动一位，并通过取模循环使用密钥，应填 `(n+1)%len(yw)`。  
（3）数字字符 `ch` 对应的对称数字为 `9-int(ch)`；也可利用字符编码写成 `57-ord(ch)`。

---

## 相关链接

- 相关知识点：
