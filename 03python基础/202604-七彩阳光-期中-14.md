---
id: 202604-七彩阳光-期中-14
题型: 填空题
来源: "[[试卷/202604-七彩阳光-期中.pdf|202604-七彩阳光-期中.pdf]]"
试卷: 202604-七彩阳光-期中.pdf
题号: "14"
难度: 中等
知识点:
  - 03python基础
  - 条件与循环
  - 状态判断
完成次数: 0
正确率:
状态: 未练习
错题原因:
创建日期: 2026-05-20
图片核验: 无图片
tags:
  - 状态/未练习
  - 知识点/03python基础
---

# 题目 14

某机器人根据红外传感器数据生成动作指令，规则如下：

① 距离 `d<=5cm` 时，指令为 `"Stop"`；距离 `d > 20cm` 时，指令为 `"Back"`；其他范围（`5 < d <= 20`），指令为 `"Forward"`。②当指令连续出现 `k` 次相同，且与最近已发送指令不同时，则发送该指令。请回答下列问题：

**（1）** 若 `k=2`，最近已发送指令为 `"Stop"`，随后生成的距离序列对应的指令依次为：`"Forward"`，`"Forward"`，`"Forward"`，`"Back"`，`"Back"`，`"Forward"`，`"Back"`，`"Back"`，则由该序列触发的指令发送次数为 ______ 次。

**（2）** 实现上述功能的部分 Python 程序如下，请在划线处填入合适的代码。

```python
last = "None"   # 最近已发送指令，初始为"None"表示未发送
cnt = 0
prev = "None"   # 上一次读取的指令
k = int(input("请输入k值: "))
while True:
    # 获取距离数据保存到变量 d 中，代码略
    if d > 20:
        code = "Back"
    elif d > 5:
        code = "Forward"
    else:
        code = "Stop"

    if code == prev:
        cnt += 1
    else:
        ____①____
        cnt = 1

    if ____②____ and cnt == k:
        # 发送指令 code，代码略
        last = code
        ____③____
    # 延时 1 秒，代码略
```

---

## 答案

**正确答案：** （1）2；（2）① `prev = code`；② `code != last`；③ `cnt = 0`

---

## 解析

（1）前两个 `"Forward"` 连续且不同于最近已发送的 `"Stop"`，触发发送 `"Forward"`；随后两个 `"Back"` 连续且不同于最近已发送的 `"Forward"`，触发发送 `"Back"`；最后两个 `"Back"` 虽连续，但与最近已发送指令相同，不再发送，因此共发送 2 次。（2）当当前指令与上一指令不同时，应更新 `prev` 并把连续计数重置为 1；发送条件还需满足当前指令不同于最近已发送指令；发送后将 `cnt` 清 0，避免同一连续段重复触发。

---

## 相关链接

- 来源：[[试卷/202604-七彩阳光-期中.pdf|202604-七彩阳光-期中.pdf]]

