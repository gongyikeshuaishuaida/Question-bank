---
id: 202511-杭州-高三-一模-13
题型: 填空题
来源: "[[试卷/202511-杭州-高三-一模.pdf|202511-杭州-高三-一模.pdf]]"
试卷: 202511-杭州-高三-一模.pdf
年级: 高三
题号: "13"
难度: 困难
水平: 选考
知识点:
  - 03python基础
  - 字符串贪心
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-04
图片核验: 无图片
tags:
  - 知识点/03python基础
---

# 题目 13

13. 有两个仅包含数字字符的字符串 s1 和 s2，它们的长度分别为 n 和 m。现要从这两个字符串中各取若干字符，字符数总和为 k（k≤n+m），重新拼接这些字符，在保持所取字符在原字符串中相对顺序不变的情况下，组成一个最大的字符串。例如，有字符串"392"和"768"，从其中取出 4 个字符能组成最大的字符串为"9782"。请回答下列问题：

（1）若字符串 s1 的值为"506"，s2 的值为"503"，则从其中取出 5 个字符能组成的最大字符串为 ▲ 。

（2）实现上述功能的部分 Python 程序如下，请在划线处填入合适的代码。

```python
def select(s, d):
    # 返回由字符串 s 中 d 个字符组成的最大字符串，其中字符的相对顺序不变，代码略

# 读取 s1、s2、n、m、k，代码略

①
for p in range(k):
    if p <= n and k-p <= m:
        x = select(s1, p)
        ②
        tmp = ""
        i, j = 0, 0
        while i < len(x) or j < len(y):
            if j >= len(y) or i < len(x) and (③):
                tmp += x[i]
                i += 1
            else:
                tmp += y[j]
                j += 1
        if tmp > res:
            res = tmp
print("组成的最大字符串为：", res)
```

---

## 答案

**正确答案：**

（1）56503（或"56503"）

（2）① `res = ""`；② `y = select(s2, k-p)`；③ `x[i:] > y[j:]`（或 `x[i] > y[j] or x[i] == y[j] and x[i:] > y[j:]`）

---

## 解析

（1）s1="506"共 3 位，s2="503"共 3 位，需取 5 位。穷举：s1 取 3 位+s2 取 2 位 → 合并时按字典序贪心选择较大的字符，得"56503"。

（2）该算法枚举从 s1 取 p 个字符、s2 取 k-p 个字符的所有可能：
- ① 初始化最优结果 `res = ""`。
- ② 从 s2 中选 k-p 个字符：`y = select(s2, k-p)`。
- ③ 合并两个字符串时，比较当前剩余子串的字典序：`x[i:] > y[j:]`，选较大的那个首字符加入结果。这保证了最终拼接字符串在保持各自相对顺序下最大。

---

## 相关链接

- 相关知识点：
