---
id: 202604-金华十校-期中-14
题型: 填空题
来源: "[[试卷/202604-金华十校-期中.pdf|202604-金华十校-期中.pdf]]"
试卷: 202604-金华十校-期中.pdf
题号: "14"
难度: 中等
知识点:
  - 03python基础
  - 传感器数据处理
  - 循环
完成次数: 0
正确率:
状态: 未练习
错题原因:
创建日期: 2026-05-19
图片核验: 无图片
tags:
  - 状态/未练习
  - 知识点/03python基础
---

# 题目 14

小张要根据采集的湿度数据分析智能系统调整检测频率的实际情况。智能系统调整检测频率的规则为：若连续 4 次采集湿度的平均值在大棚适宜湿度值范围（适宜湿度值±阈值）内，且湿度波动（最大值-最小值）不超过阈值，则检测频率为 1 次/分钟（稳定状态），否则将检测频率提升为 5 次/分钟（波动状态）。

**（1）** 已知初始状态为稳定状态，最近 7 次的湿度数据为 `[64, 66, 66, 68, 72, 68, 68]`，大棚适宜湿度值为 70，阈值为 5，在此期间修改 ______ 次检测频率。

**（2）** 分析并统计调整检测频率次数的 Python 程序如下，请在划线处填入合适的代码。

```python
# 湿度数据保存在列表 hum 中，例如：hum = [64, 66, 66, 68, 72, 68, 68,......]，代码略
m = 4      # 连续 4 次采集
s = 70     # 大棚适宜湿度值
T = 5      # 阈值
cnt = 0
pre_state = "1 次/分钟(稳定状态)"

for i in range(0, len(hum) - 1):   # ①加框处
    cur = hum[i : i + m]
    tot = 0
    for num in cur:
        tot += num
    avg = tot / len(cur)
    bd = max(cur) - min(cur)
    if s - T <= avg <= s + T and ____②____:
        state = "1 次/分钟(稳定状态)"
    else:
        state = "5 次/分钟(波动状态)"
    if ____③____:
        cnt += 1
    pre_state = state

print("调整检测频率次数:", cnt)
```

①加框处代码有错，应修改为 ______；②划线处代码为 ______；③划线处代码为 ______。

---

## 答案

**正确答案：** （1）2；（2）① `len(hum)-m+1`；② `bd <= T`；③ `state != pre_state`

---

## 解析

长度为 `m` 的连续窗口起点最大为 `len(hum)-m`，因此 `range` 的结束值应为 `len(hum)-m+1`。稳定状态要求平均值在范围内且波动不超过阈值，所以②为 `bd <= T`。只有当前状态和上一状态不同时才表示调整了检测频率，所以③为 `state != pre_state`。给定 7 个数据形成 4 个窗口，状态变化为稳定到波动、波动到稳定，共修改 2 次。

---

## 相关链接

- 来源：[[试卷/202604-金华十校-期中.pdf|202604-金华十校-期中.pdf]]

