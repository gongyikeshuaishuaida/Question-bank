---
id: 202606-衢州-高二-期末-12
题型: 选择题
来源: "[[试卷/202606-衢州-高二-期末.pdf|202606-衢州-高二-期末.pdf]]"
试卷: 202606-衢州-高二-期末.pdf
年级: 高二
题号: "12"
难度: 困难
水平: 选考
知识点:
  - 13查找与排序
  - 二分查找
  - 查找次数
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-30
图片核验: 无图片
tags:
  - 知识点/13查找与排序
  - 知识点/二分查找
  - 知识点/查找次数
---

# 题目 12

12. 某二分查找的 Python 程序段如下：

```python
import random
key = random.randint(10, 30)  # 随机生成一个10-30之间的整数
i = 0
j = len(a) - 1
cnt = 0
while i <= j:
    m = (i + j) // 2
    cnt += 1
    if a[m] == key:
        break
    if a[m] > key:
        j = m - 1
    else:
        i = m + 1
```

当 `a` 为 `[10,13,16,18,22,25,27,28,30]`，运行该程序段查找 `key`，`cnt` 的值为 3；若将 `a` 修改为 `[13,17,18,19,20,21,22,28,30]`，查找同一 `key` 值，则 `cnt` 的值为（   ）

- A. 1
- B. 2
- C. 3
- D. 4

---

## 答案

**正确答案：** C

---

## 解析

原数组第一次比较 22，第二次比较 13 或 27。已知共比较 3 次，可确定 `key` 位于第三层对应的取值范围。新数组第一次比较 20，第二次比较 17 或 22；对所有满足原数组比较 3 次的同一 `key`，第三次比较后均会命中或结束查找，因此新的 `cnt` 仍为 3。

---

## 相关链接

- 相关知识点：
