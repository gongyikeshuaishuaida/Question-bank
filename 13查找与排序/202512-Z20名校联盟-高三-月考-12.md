---
id: 202512-Z20名校联盟-高三-月考-12
题型: 选择题
来源: "[[试卷/202512-Z20名校联盟-高三-月考-试卷.pdf|202512-Z20名校联盟-高三-月考-试卷.pdf]]"
试卷: 202512-Z20名校联盟-高三-月考-试卷.pdf
年级: 高三
题号: "12"
难度: 中等
水平: 选考
知识点:
  - 13查找与排序
  - 二分查找
  - 山脉数组
完成次数: 0
补课:
  - 胡佳钰
  - 周振轩
正确率:
错题原因:
创建日期: 2025-12
图片核验: 无图片
tags:
  - 知识点/13查找与排序
  - 知识点/二分查找
  - 知识点/山脉数组
---
# 题目 12

**12．** 已知一个长度为 `n`（`n >= 3`）的整数数组 `a` 满足以下条件：数组元素大小呈先升后降，仅存在一个索引 `i` (`0 < i < n-1`) 使得 `a[i-1] < a[i]`，且 `a[i] > a[i+1]`。这样的数组被称为山脉数组。定义以下函数实现在山脉数组中找到峰值元素（即最大值）的索引。

```python
def search(a):
    left, right = 0, len(a) - 1
    while left < right:
        mid = (left + right) // 2
        if      ①      :
            ②
        else:
            ③
    return left
```

划线①②③处应填入的代码为（   ）

- A. ① `a[mid] > a[mid + 1]` ② `right = mid + 1` ③ `left = mid`
- B. ① `a[mid] < a[mid + 1]` ② `left = mid + 1` ③ `right = mid`
- C. ① `a[mid] < a[mid + 1]` ② `right = mid + 1` ③ `left = mid`
- D. ① `a[mid] > a[mid + 1]` ② `left = mid + 1` ③ `right = mid`

---

## 答案

**正确答案：** B

---

## 解析

山脉数组先升后降，峰值在上升段的最右端（或下降段的最左端）。

二分查找峰值策略：比较 `a[mid]` 和 `a[mid+1]`：
- 若 `a[mid] < a[mid+1]`：说明 mid 在上升段，峰值在右侧 → `left = mid + 1`
- 若 `a[mid] > a[mid+1]`：说明 mid 在下降段（或就是峰值），峰值在左侧（含 mid）→ `right = mid`

① `a[mid] < a[mid + 1]`：判断是否在上升段。
② `left = mid + 1`：峰值在右侧。
③ `right = mid`：峰值在左侧（含 mid）。

选项 A/D 用 `a[mid] > a[mid+1]` 反转了逻辑会导致错误的收缩方向。
选项 C 的②③赋值错误。

---

## 相关链接

- 类似知识点题：[[13查找与排序/202506-宁波九校-高二-期末-12]]、[[13查找与排序/202512-嘉兴-高三-一模-12]]、[[13查找与排序/202512-强基联盟-高三-月考-12]]、[[13查找与排序/202512-金丽衢十二校-高三-一模-12]]
- 相关知识点：[[13查找与排序]]
