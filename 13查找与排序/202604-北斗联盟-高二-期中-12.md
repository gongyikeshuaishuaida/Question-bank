---
id: 202604-北斗联盟-高二-期中-12
题型: 选择题
来源: "[[试卷/202604-北斗联盟-高二-期中.pdf|202604-北斗联盟-高二-期中.pdf]]"
试卷: 202604-北斗联盟-高二-期中.pdf
年级: 高二
题号: "12"
难度: 中等
水平: 选考
知识点:
  - 13查找与排序
  - 选择排序
  - 03python基础
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-30
图片核验: 无图片
tags:
  - 知识点/13查找与排序
  - 知识点/选择排序
  - 知识点/03python基础
---

# 题目 12

将列表 `d` 内数据进行升序排序，实现该功能的 Python 程序段如下：

```python
def f(a):
    num = len(a)
    for p in range(num - 1):
        t = p
        for q in ____①____:
            if a[q] <= a[t]:
                ____②____
        a[p], a[t] = a[t], a[p]
    return a

d = [64, 25, 12, 22, 11]
print("排序结果为:", f(d))
```

则划线处应填入的代码依次为（   ）

- A. ① `range(p, num-1)`　② `t = q + 1`
- B. ① `range(p + 1, num)`　② `t = q + 1`
- C. ① `range(p, num-1)`　② `t = q`
- D. ① `range(p + 1, num)`　② `t = q`

---

## 答案

**正确答案：** D

---

## 解析

选择排序每轮将位置 `p` 之后的最小元素换到 `p`。内层循环应遍历 `p+1` 到列表末尾，即 `range(p + 1, num)`；发现更小元素时用 `t = q` 记录其下标。

---

## 相关链接

- 相关知识点：
