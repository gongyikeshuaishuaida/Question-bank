---
id: 202512-金丽衢十二校-高三-一模-12
题型: 选择题
来源: "[[试卷/202512-金丽衢十二校-高三-一模.pdf|202512-金丽衢十二校-高三-一模.pdf]]"
试卷: 202512-金丽衢十二校-高三-一模.pdf
年级: 高三
题号: "12"
难度: 中等
水平: 选考
知识点:
  - 13查找与排序
  - 二分查找
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-01
图片核验: 无图片
tags:
  - 知识点/13查找与排序
  - 知识点/二分查找
---

# 题目 12

12. 有如下 Python 程序：

```python
import random

a = [1, 3, 3, 3, 4, 5]
i = 0
j = len(a) - 1
c = 0
n = len(a)
flag = False
key = random.randint(0, 4)
while i <= j and not flag:
    m = (i + j) // 2
    c += 1
    if a[m] == key:
        flag = True
    if a[m] > key:
        j = m - 1
    else:
        i = m + 1
print(n - i)
```

下列说法不正确的是（   ）
- A. 若 `c` 的值为 3，则 `key` 的值只能为 2
- B. 若 `key` 的值为 3，退出时 `i` 的值为 3
- C. 输出 `n-i` 代表大于 `key` 的数据个数
- D. 若 `key` 在列表 `a` 中不存在，则必有 `i = j + 1`

---

## 答案

**正确答案：** C

---

## 解析

程序找到 `key` 后也会执行第二个 `if...else` 更新 `i` 或 `j`，因此 `n-i` 并不总是严格表示大于 `key` 的数据个数。例如 `key=3` 时可能找到中间的 3 后令 `i=3`，输出 `3`，但大于 3 的元素只有 4、5 两个。

---

## 相关链接

- 类似知识点题：[[13查找与排序/202506-宁波九校-高二-期末-12]]、[[13查找与排序/202512-Z20名校联盟-高三-月考-12]]、[[13查找与排序/202512-嘉兴-高三-一模-12]]、[[13查找与排序/202512-强基联盟-高三-月考-12]]
- 来源：[[试卷/202512-金丽衢十二校-高三-一模.pdf|202512-金丽衢十二校-高三-一模.pdf]]
