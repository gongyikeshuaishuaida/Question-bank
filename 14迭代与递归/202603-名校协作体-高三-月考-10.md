---
id: 202603-名校协作体-高三-月考-10
题型: 选择题
来源: "[[试卷/202603-名校协作体-高三-月考.pdf|202603-名校协作体-高三-月考.pdf]]"
试卷: 202603-名校协作体-高三-月考.pdf
年级: 高三
题号: "10"
难度: 中等
知识点:
  - 14迭代与递归
  - 递归
完成次数: 0
正确率:
状态: 未练习
错题原因:
创建日期: 2026-06-30
图片核验: 无图片
tags:
  - 状态/未练习
  - 知识点/14迭代与递归
  - 知识点/递归
---

# 题目 10

10. 对于任意正整数 `n`，甲、乙程序段输出结果相同，则乙程序段划线处的正确代码为（   ）

甲程序段：

```python
k = 1
for i in range(n % 2, n, 2):
    k = k * (i + 2)
print(k)
```

乙程序段：

```python
def f(n):
    if      ①      :
        return n
    else:
        return      ②
print(f(n))
```

- A. ① `n < 2`；② `n * f(n + 2)`
- B. ① `n < 2`；② `n * f(n - 2)`
- C. ① `n <= 2`；② `n * f(n + 2)`
- D. ① `n <= 2`；② `n * f(n - 2)`

---

## 答案

**正确答案：** D

---

## 解析

甲程序段按 `n` 的奇偶性计算 `2×4×...×n` 或 `3×5×...×n`，递归边界应为 `n<=2` 时返回 `n`，递推关系为 `f(n)=n*f(n-2)`，因此选择 D。

---

## 相关链接

- 来源：[[试卷/202603-名校协作体-高三-月考.pdf|202603-名校协作体-高三-月考.pdf]]
