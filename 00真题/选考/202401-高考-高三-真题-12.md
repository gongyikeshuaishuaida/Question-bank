---
id: 202401-高考-高三-真题-12
题型: 选择题
来源: "[[试卷/202401-高考-高三-真题.pdf|202401-高考-高三-真题.pdf]]"
试卷: 202401-高考-高三-真题.pdf
年级: 高三
题号: "12"
难度: 困难
水平: 选考
知识点:
  - 11链表
  - 链表重排
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-27
图片核验: 已通过
tags:
  - 知识点/11链表
  - 知识点/链表重排
---

# 题目 12

使用列表 d 模拟链表结构（节点数大于 0），每个节点包含数据区域和指针区域，h 为头指针。链表中各节点已按数据区域中数值的绝对值由小到大排列，如第 12 题图 a 所示。现要修改该链表各节点的链接关系，使链表各节点按数据区域中的数值由小到大排列，结果如第 12 题图 b 所示。实现该功能的程序段如下，方框中应填入的正确代码为（   ）

![[attachments/202401高考_12_图1.jpg]]

![[attachments/202401高考_12_图2.jpg]]

```python
t = h
p = d[h][1]
while p != -1:
    q = d[p][1]
    # 方框处
    p = q
d[t][1] = -1
```

- A.

  ```python
  if d[p][0] > 0:
      d[q][1] = p
      d[t][1] = q
  else:
      d[h][1] = q
      h = p
  ```

- B.

  ```python
  if d[p][0] > 0:
      d[t][1] = q
      t = q
  else:
      h = p
      d[p][1] = t
  ```

- C.

  ```python
  if d[p][0] > 0:
      d[t][1] = p
      t = p
  else:
      d[p][1] = h
      h = p
  ```

- D.

  ```python
  if d[p][0] > 0:
      d[t][1] = q
      d[q][1] = p
  else:
      d[p][1] = h
      h = q
  ```

---

## 答案

**正确答案：** C

---

## 解析

原链表按绝对值递增。遍历时，正数节点应接到链表尾部，并更新尾指针 t；负数节点应插入链表头部，并更新头指针 h。这样负数部分被逆序到前端、正数部分保持原顺序，最终得到按数值递增的链表，符合 C。

---
