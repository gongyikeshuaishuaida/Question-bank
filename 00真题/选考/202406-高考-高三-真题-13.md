---
id: 202406-高考-高三-真题-13
题型: 填空题
来源: "[[试卷/202406-高考-高三-真题.pdf|202406-高考-高三-真题.pdf]]"
试卷: 202406-高考-高三-真题.pdf
年级: 高三
题号: "13"
难度: 中等
水平: 选考
知识点:
  - 03python基础
  - 列表遍历
  - 状态检测
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-27
图片核验: 无图片
tags:
  - 知识点/03python基础
  - 知识点/列表遍历
  - 知识点/状态检测
---

# 题目 13

某监控设备可定时采集红绿信号灯状态数据，数据格式记为 `[a,b]`，其中 a、b 分别为红灯和绿灯的状态值，0 表示灯灭，1 表示灯亮，如 `[0,1]` 表示红灯灭、绿灯亮。

现要编写程序，每隔 1 秒采集并检测信号灯是否存在如下异常状态：第一类，红绿灯同亮或同灭；第二类，红灯或绿灯超时，即保持同一状态时长大于上限值（如 300 秒）。检测到异常状态就发送相应信息。请回答下列问题：

(1) 若检测到“红绿灯同亮”异常，则采集到的数据是 ▲（单选，填字母）。

- A. `[0,0]`
- B. `[0,1]`
- C. `[1,0]`
- D. `[1,1]`

(2) 实现上述功能的部分 Python 程序如下，请在划线处填入合适的代码。

```python
tlimit = 300  # 设置信号灯保持同一状态时长上限值
pre = [-1, -1]
t = [0, 0]  # t[0]、t[1]分别记录红灯、绿灯保持同一状态的时长
while True:
    # 接收一次采集到的状态数据，存入d，代码略
    if ①:
        if d[0] == 1:
            # 发送"红绿灯同亮"信息，代码略
        else:
            # 发送"红绿灯同灭"信息，代码略
    for i in ②:
        if d[i] == pre[i]:
            t[i] += 1
            if ③:
                if i == 0:
                    # 发送"红灯超时"信息，代码略
                else:
                    # 发送"绿灯超时"信息，代码略
        else:
            t[i] = 1
    pre = d
    # 延时1秒，代码略
```

---

## 答案

(1) D

(2) ① `d[0]==d[1]`；② `range(2)`（或 `range(len(pre))`、`range(len(d))`）；③ `t[i]>tlimit`

---

## 解析

红绿灯同亮对应 `[1,1]`。第一类异常的共同特征是两个状态值相等，因此用 `d[0]==d[1]` 判断；红灯和绿灯分别对应下标 0、1，可用 `range(2)` 遍历；同一状态累计时长超过上限时，条件为 `t[i]>tlimit`。

---
