---
id: 202501-高考-高三-真题-13
题型: 填空题
来源: "[[试卷/202501-高考-高三-真题.pdf|202501-高考-高三-真题.pdf]]"
试卷: 202501-高考-高三-真题.pdf
年级: 高三
题号: "13"
难度: 中等
水平: 选考
知识点:
  - 03python基础
  - 擂台法
  - 计数器
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-27
图片核验: 无图片
tags:
  - 知识点/03python基础
  - 知识点/擂台法
  - 知识点/计数器
---

# 题目 13

根据机器的负载率对工厂的 6 台机器（编号 0～5）进行监控和调度，调度规则是：每隔 1 小时采集 1 次各台机器的负载率（负载率用百分制表示，机器休息时的负载率为 0），负载率超过 90 的机器都调度休息；如果所有机器负载率都不超过 90，则负载率最高的机器休息，若多台机器负载率同为最高，则编号最小的机器休息。休息的机器在休息 1 小时后再次工作。

(1) 若某次采集到 0～5 号机器的负载率依次为 75、85、88、0、88、87，当前处于休息状态的机器编号是 3，接下来休息的机器编号是 ▲。

(2) 实现上述功能的部分 Python 程序如下，请在划线处填入合适的代码。

```python
n = 6
a = [0] * n
# 启动0～4号机器工作，5号机器休息，代码略
while True:
    # 延时1小时，再采集各机器负载率存入a，代码略
    t = 0
    cnt = 0
    for i in range(n):
        if ①:
            # 调度i号机器休息，代码略
            cnt += 1
        if a[i] > a[t]:
            ②
        elif a[i] == 0:
            # 调度i号机器工作，代码略
    if ③:
        # 调度t号机器休息，代码略
```

---

## 答案

(1) 2

(2) ① `a[i]>90`；② `t=i`；③ `cnt==0`

---

## 解析

(1) 没有机器的负载率超过 90，故应选择负载率最高的机器休息。2 号和 4 号机器均为 88，按编号较小者优先，答案为 2。

(2) cnt 统计负载率超过 90 的机器数；t 用擂台法记录当前最大负载率对应的机器编号。只有 `cnt==0` 时，才需要让 t 号机器休息。

---
