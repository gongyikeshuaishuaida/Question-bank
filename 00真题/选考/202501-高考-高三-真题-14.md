---
id: 202501-高考-高三-真题-14
题型: 填空题
来源: "[[试卷/202501-高考-高三-真题.pdf|202501-高考-高三-真题.pdf]]"
试卷: 202501-高考-高三-真题.pdf
年级: 高三
题号: "14"
难度: 中等
水平: 选考
知识点:
  - 06信息系统
  - 04数据处理
  - pandas
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-27
图片核验: 已通过
tags:
  - 知识点/06信息系统
  - 知识点/04数据处理
  - 知识点/pandas
---

# 题目 14

某研究小组搭建了室外温度监测系统，在 4 所学校各设置了 1 个监测点。智能终端连接传感器，每隔 3 小时采集 1 次温度数据，通过网络将温度数据传输到服务器。服务器根据数据判断出异常情况时，通过智能终端控制执行器发出预警信号。请回答下列问题：

(1) 在搭建该监测系统时，温度传感器与智能终端的配备总数量合理的是 ▲（单选，填字母）。

- A. 4 个温度传感器和 4 个智能终端
- B. 4 个温度传感器和 1 个智能终端

(2) 系统数据采集的时间间隔为 3 小时，可用于控制采集时间的设备是 ▲（单选，填字母）。

- A. 传感器
- B. 智能终端

(3) 编写智能终端程序时，不需要知道 ▲（多选，填字母）。

- A. 与传感器连接的智能终端引脚
- B. 服务器的存储容量
- C. 服务器的地址及端口
- D. 数据库的文件名

(4) 当服务器判定有异常情况后，除可通过智能终端控制蜂鸣器发出预警声音之外，请写出其他两种预警的具体方式。

(5) 将系统中某年的数据导出到文件 `data.xlsx` 中。“预报温度”列是学校所在地的天气预报温度数据，“差值”列是实测温度与预报温度相减的绝对值。现要由高到低输出 3 月份各监测点差值的平均值，再用差值平均值最高的监测点的 3 月份实测温度数据绘制线形图。

输出的差值平均值如下：

![[attachments/202501高考_14_图1.jpg]]

差值平均值最高的监测点的 3 月份实测温度线形图如下：

![[attachments/202501高考_14_图2.jpg]]

实现上述功能的部分 Python 程序如下，请选择合适的代码填入划线处（填字母）。

```python
import pandas as pd
import matplotlib.pyplot as plt

df = pd.read_excel("data.xlsx")
df1 = ①
df2 = ②
df2 = ③
# 依次输出df2中各监测点编号及其差值的平均值，代码略
# 将df2中首行的监测点编号存入uid，代码略
df2 = ④
# 创建x列表，元素依次为0～247，代码略
plt.plot(x, df2["实测温度"])
```

①②③④处可选代码有：

- A. `df.groupby("月",as_index=False).差值.mean()`
- B. `df[df["监测点"]==uid]`
- C. `df[df["月"]==3]`
- D. `df1.sort_values("差值",ascending=False)`
- E. `df1.groupby("监测点",as_index=False).差值.mean()`
- F. `df1[df1["监测点"]==uid]`
- G. `df2.sort_values("差值",ascending=False)`

---

## 答案

(1) A

(2) B

(3) BD

(4) 示例：通过智能终端控制 LED 灯闪烁；服务器发送预警邮件。

(5) ① C；② E；③ G；④ F

---

## 解析

(1) 每个监测点都需要独立的传感器和智能终端，共各 4 个。

(2) 采集时间间隔由智能终端中的程序控制。

(3) 智能终端程序需要知道传感器连接引脚以及服务器地址和端口；服务器存储容量和数据库文件名属于服务器端配置。

(4) 预警既可由智能终端控制其他执行器实现，也可由服务器通过邮件、短信等方式发送。

(5) 先用 C 筛选 3 月数据，再用 E 按监测点分组求差值平均值，用 G 按差值降序排序，最后用 F 从 3 月数据中筛选目标监测点的实测温度。

---
