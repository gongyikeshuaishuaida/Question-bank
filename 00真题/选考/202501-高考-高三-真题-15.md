---
id: 202501-高考-高三-真题-15
题型: 填空题
来源: "[[试卷/202501-高考-高三-真题.pdf|202501-高考-高三-真题.pdf]]"
试卷: 202501-高考-高三-真题.pdf
年级: 高三
题号: "15"
难度: 困难
水平: 选考
知识点:
  - 13查找与排序
  - 二分查找
  - 11链表
  - 排名统计
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-27
图片核验: 无图片
tags:
  - 知识点/13查找与排序
  - 知识点/二分查找
  - 知识点/11链表
  - 知识点/排名统计
---

# 题目 15

某市举行体育赛事活动，n 所学校的选手已完成预赛，现计划根据预赛成绩挑选 s 名选手参加市决赛。成绩位列所在学校前 w 名次的选手直接入选，剩余名额按成绩由高到低依次挑选，成绩相同的选手一并入选，选中的选手数一旦达到或超过 s 名，挑选结束。

成绩数据表已按成绩由高到低排列。选手校内名次的计算方法是：若选手所在学校有 m 人成绩高于该选手，则该选手的名次为 $m+1$。

样例数据如下：

| 学校编号 | 0 | 2 | 2 | 0 | 0 | 2 | 2 | 0 | 1 | 1 | 1 | 1 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 选手编号 | 0002 | 2027 | 2002 | 0072 | 0182 | 2071 | 2128 | 0012 | 1081 | 1002 | 1008 | 1208 |
| 成绩 | 198 | 185 | 183 | 182 | 182 | 177 | 177 | 176 | 175 | 163 | 161 | 161 |
| 校内名次 | 1 | 1 | 2 | 2 | 2 | 3 | 3 | 4 | 1 | 2 | 3 | 3 |
| 是否入选 | True | True | True | True | True | True | True | False | True | True | False | False |

(1) 对于表中前 4 行数据，若 s、w 分别为 5 和 1，则 0 号学校入选人数是 ▲。

(2) 定义如下 `search(data,sid,score)` 函数。data 列表每个元素的前 5 个数据项依次为学校编号、选手编号、成绩、校内名次、是否入选，列表已按成绩由高到低排列。函数查找选手编号为 sid、成绩为 score 的元素，返回其下标，若未找到则返回 -1。

```python
def search(data, sid, score):
    left, right = 0, len(data) - 1
    f = -1
    while left <= right:
        mid = (left + right) // 2
        if score == data[mid][2]:
            f = mid
            left = mid + 1
        elif score < data[mid][2]:
            left = mid + 1
        else:
            right = mid - 1
    if f == -1:
        return -1
    for i in range(f, len(data)):  # 加框处
        if data[i][2] != score:
            return -1
        elif data[i][1] == sid:
            return i
    return -1
```

① 若成绩依次为 `198,185,183,182,182,177,177,176,175,163,161,161`，score 为 177，则 while 语句中循环体的执行次数是 ▲。

② 程序中加框处代码有错，请改正。

(3) 实现计算校内名次以及挑选决赛选手功能的部分 Python 程序如下，请在划线处填入合适的代码。

```python
def proc(data, n, s, w):
    # 创建r列表，共n个元素，每个元素均为[0,0,201]，代码略
    heads = [-1, -1]
    tails = [-1, -1]
    cnt = 0
    for i in range(len(data)):
        ①
        r[k][1] += 1
        if data[i][2] < r[k][2]:
            r[k][2] = data[i][2]
            ②
        data[i][3] = r[k][0]
        data[i].append(-1)
        v = 1
        if data[i][3] <= w:
            data[i][4] = True
            cnt += 1
            v = 0
        if heads[v] == -1:
            heads[v] = i
        else:
            data[tails[v]][5] = i
        tails[v] = i

    p, q = heads[0], heads[1]
    res = []
    while cnt < s and q != -1:
        tmp = data[q][2]
        while q != -1 and data[q][2] == tmp:
            ③:
                res.append(data[p][1])
                p = data[p][5]
            res.append(data[q][1])
            data[q][4] = True
            cnt += 1
            q = data[q][5]
    while p != -1:
        res.append(data[p][1])
        p = data[p][5]
    return res
```

---

## 答案

(1) 3

(2) ① 4；② 将 `range(f,len(data))` 改为 `range(f,-1,-1)`

(3) ① `k=data[i][0]`；② `r[k][0]=r[k][1]`；③ `while p!=-1 and p<q`

---

## 解析

(1) 各校第 1 名先直接入选，随后按成绩继续选择。0 号学校两名同为 182 分的选手必须一并入选，因此 0 号学校共有 3 人入选。

(2) 二分查找的 mid 依次为 5、8、6、7，共执行 4 次。由于 f 最终指向成绩等于 score 的区间右端点，查找选手编号时应从 f 向前遍历。

(3) k 是当前选手的学校编号；`r[k][1]` 是累计人数，当成绩降低时，该值就是新成绩对应的校内名次；p、q 分别遍历直接入选链表和候选链表，为保持原成绩表顺序，应在 `p<q` 时先输出 p 所指选手。

---
