---
id: 202604-杭州-高三-二模-15
题型: 填空题
来源: "[[试卷/202604-杭州-高三-二模.pdf|202604-杭州-高三-二模.pdf]]"
试卷: 202604-杭州-高三-二模.pdf
年级: 高三
题号: "15"
难度: 困难
水平: 选考
知识点:
  - 02算法
  - 二分查找
  - 调度算法
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-06-29
图片核验: 已通过
tags:
  - 知识点/02算法
  - 知识点/二分查找
  - 知识点/调度算法
---

# 题目 15

15. 某物流仓库提供货物搬运功能，每件货物规格相同，仓库内共有 n 辆叉车（编号 1～n）。收到若干订单，每个订单包括装货时间（从 0 开始编号）和货物数量。根据叉车调度规则，要求在截止时间 t 前完成所有订单的货物搬运，计算需要的最少叉车数量。

规则：每辆叉车一次处理一个订单的货物，且一次最多装载 10 件；获取每件货物花费 1 单位时间，每次将叉车上的所有货物搬运至货车花费 3 单位时间。

叉车调度规则：

① 选择最早空闲叉车；若空闲叉车多辆，选择编号最小。  
② 若一辆叉车无法在截止时间 t 前完成搬运，则在每辆叉车尽可能多装货物（一次不超过 10 件）的前提下，安排最少数量叉车在截止时间 t 前完成；若不能，输出“订单无法完成！”。

例如 t 为 15，data 为 `[[0,22],[4,12]]`。当叉车数量为 4 时，根据调度规则，货物搬运过程如图 a 所示。

![[attachments/202604杭州高三二模_15_图1.png]]

(1) 若 data 为 `[[0,15],[4,12]]`，t 为 15，则完成所有订单至少需要 ____▲____ 辆叉车。

(2) 定义如下 `psort(data)` 函数，data 列表的每个元素包含 2 个数据项，依次为装货时间和货物数量，已按装货时间升序排列。函数的功能为保持订单按装货时间升序排列不变，当装货时间相同时，按货物数量降序排列，返回 data。

```python
def psort(data):
    for i in range(len(data) - 1):
        for j in range(len(data) - i - 1):
            if ____▲____:
                data[j], data[j + 1] = data[j + 1], data[j]
    return data
```

要实现函数功能，方框处应填入的代码为 ____▲____（单选，填字母）。

- A. `data[j][1] < data[j+1][1]`  
- B. `data[j][1] > data[j+1][1]`  
- C. `data[j][0] == data[j+1][0] and data[j][1] < data[j+1][1]`  
- D. `data[j][0] != data[j+1][0] and data[j][1] > data[j+1][1]`

(3) 实现计算最少叉车数量的部分 Python 程序如下，请在划线处填入合适的代码。

```python
def findi(cars):
    mini = 0
    for j in range(len(cars)):
        if cars[j] < cars[mini]:
            mini = j
    return mini

def check(data, m, v, t):
    cars = [0] * m
    for i in range(len(data)):
        ____①____
        while count > 0:
            mini = findi(cars)
            stime = max(cars[mini], data[i][0])
            remain = t - 3 - stime
            if remain <= 0:
                return False
            load = min(count, remain, v)
            ____②____
            count -= load
    return True

"""读取截止时间 t、叉车数量 n；读取订单数据表存入 data 列表，每个元素包含 2 个数据项，依次为装货时间和货物数量。代码略。"""
data = psort(data)
v = 10
left, right = 1, n
while left <= right:
    m = (left + right) // 2
    if check(data, m, v, t):
        right = m - 1
    else:
        left = m + 1
if ____③____:
    print("订单无法完成！")
else:
    print("最少需要", left, "辆叉车")
```

---

## 答案

**正确答案：** (1) 3；(2) C；(3) ① `count = data[i][1]`；② `cars[mini] = stime + load + 3`；③ `left > n`

---

## 解析

第 (1) 小题中 2 辆叉车无法在 t=15 前完成第二个订单，3 辆可以完成。排序只在装货时间相同时按货物数量降序交换，因此选 C。`check` 中先取订单货物数量，安排后更新该叉车下次空闲时间；二分结束后若 `left > n`，说明所有 n 辆叉车仍无法完成。

---

## 相关链接

- 类似知识点题：[[09队列/202406-宁波九校-高二-期末-15]]、[[08数组/202511-绍兴-高三-一模-15]]、[[11链表/202604-绍兴-高三-二模-15]]、[[13查找与排序/202506-宁波九校-高二-期末-12]]、[[13查找与排序/202512-Z20名校联盟-高三-月考-12]]
- 来源：[[试卷/202604-杭州-高三-二模.pdf|202604-杭州-高三-二模.pdf]]
