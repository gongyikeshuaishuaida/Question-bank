---
id: 改编队列02
题型: 选择题
来源: "[[03python基础/202604-宁波-高三-二模-14|202604-宁波-高三-二模-14]]"
试卷: 改编出题
年级: 高三
题号: "02"
难度: 中等
水平: 选考
知识点:
  - 09队列
  - 滑动窗口
  - 列表
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-06-26
图片核验: 无图片
tags:
  - 知识点/09队列
  - 知识点/滑动窗口
---

# 改编队列02

某监测系统需找出异常波动点最密集的连续 1 小时时段。一天的数据按时间顺序存储在列表 `a` 中，每 5 分钟采集一次，因此连续 1 小时对应 12 个采集时刻。`flag[i]` 表示第 `i` 个时刻是否异常。如下程序用于输出异常点最多时段的起始时间，划线处代码依次应为（   ）

```python
n = len(a)
flag = [0] * n
for i in range(n):
    if a[i][2] > std:
        flag[i] = 1
start_time = cnt = 0
for i in range(12):
    ______①______
max_cnt = cnt
for i in range(1, n - 11):
    cnt = cnt - flag[i - 1] + ______②______
    if cnt > max_cnt:
        max_cnt = cnt
        ______③______
```

- A. ① `cnt += flag[i]`  ② `flag[i+11]`  ③ `start_time = i`  
- B. ① `cnt = flag[i]`  ② `flag[i+12]`  ③ `start_time += 1`  
- C. ① `cnt += a[i][2]`  ② `flag[i-1]`  ③ `max_cnt = i`  
- D. ① `cnt += flag[i]`  ② `flag[i+12]`  ③ `start_time = i-1`

---

## 答案

**正确答案：** A

---

## 解析

先统计第一个长度为 12 的窗口异常点数量；窗口右移时减去离开的 `flag[i-1]`，加上新进入的 `flag[i+11]`。只有出现更大值时才记录当前起点 `i`，数量相同则保留最早时段。

---

## 相关链接

- 改编自：[[03python基础/202604-宁波-高三-二模-14|202604-宁波-高三-二模-14]]
