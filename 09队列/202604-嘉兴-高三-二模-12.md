---
id: 202604-嘉兴-高三-二模-12
题型: 选择题
来源: "[[试卷/202604-嘉兴-高三-二模.pdf|202604-嘉兴-高三-二模.pdf]]"
试卷: 202604-嘉兴-高三-二模.pdf
年级: 高三
题号: "12"
难度: 中等
知识点:
  - 09队列
  - 滑动窗口
完成次数: 0
正确率:
状态: 未练习
错题原因:
创建日期: 2026-06-27
图片核验: 无图片
tags:
  - 状态/未练习
  - 知识点/09队列
  - 知识点/滑动窗口
---

# 题目 12

某连锁超市每小时统计营业额，需找出连续 3 小时的最高营业额。已知 7 小时营业额为 `money = [980, 720, 900, 850, 770, 950, 800]`，连续 3 小时最高营业额依次为：980、900、900、950、950。实现该功能的程序段如下：

```python
money = [980, 720, 900, 850, 770, 950, 800]
n = len(money); index = [0] * 100
p1 = 0; p2 = 0; k = 3
ans = []
for i in range(n):
    if p1 != p2 and index[p1] <= i - k:
        ____①____
    while p1 != p2 and money[index[p2 - 1]] <= money[i]:
        ____②____
    ____③____
    p2 += 1
    if i >= k - 1:
        ans.append(money[index[p1]])
print("每连续 3 小时的最高营业额为：", ans)
```

划线处依次填入的代码为（   ）

- A. ① `p1 += 1` ② `p2 -= 1` ③ `index[p2] = index[i]`
- B. ① `p1 += 1` ② `p2 -= 1` ③ `index[p2] = i`
- C. ① `p2 += 1` ② `p1 -= 1` ③ `index[p2] = index[i]`
- D. ① `p2 += 1` ② `p1 -= 1` ③ `index[p2] = i`

---

## 答案

**正确答案：** B

---

## 解析

`index[p1:p2]` 维护候选下标队列。若队首下标已经滑出窗口，应执行 `p1 += 1`；从队尾删除不大于当前营业额的下标，应执行 `p2 -= 1`；最后把当前下标 `i` 放入队尾，即 `index[p2] = i`。

---

## 相关链接

- 来源：[[试卷/202604-嘉兴-高三-二模.pdf|202604-嘉兴-高三-二模.pdf]]

