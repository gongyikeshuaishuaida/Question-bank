---
id: 202606-金华十校-高二-期末-09
题型: 选择题
来源: "[[试卷/202606-金华十校-高二-期末.pdf|202606-金华十校-高二-期末.pdf]]"
试卷: 202606-金华十校-高二-期末.pdf
年级: 高二
题号: "09"
难度: 中等
水平: 选考
知识点:
  - 09队列
  - 10栈
  - 栈与队列
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-28
图片核验: 无图片
tags:
  - 知识点/09队列
  - 知识点/10栈
  - 知识点/栈与队列
---

# 题目 09

9. 有空队列 `Q`、空栈 `S` 和数字序列 `a=[1,2,3,4,5]`，逐个取出序列 `a` 中的元素（以 `x` 表示）并执行如下操作。

若队列 `Q` 为空，`x` 直接入队，否则按照如下规则处理：

- 若 `x` 是偶数，队列 `Q` 中所有元素出队并依次入栈 `S`，`x` 入队；
- 若 `x` 是奇数，栈 `S` 中所有元素出栈并依次入队列 `Q`，`x` 入栈。

上述操作完成后，队列 `Q` 的队首元素和栈 `S` 的栈顶元素，分别是（   ）

- A. 5 4
- B. 4 5
- C. 5 1
- D. 1 5

---

## 答案

**正确答案：** B

---

## 解析

依次处理 1～5：处理 1 后 `Q=[1]`；处理 2 后 `Q=[2]，S=[1]`；处理 3 后 `Q=[2,1]，S=[3]`；处理 4 后 `Q=[4]，S=[3,2,1]`；处理 5 时栈中元素依次出栈进入队列，最后 `Q=[4,1,2,3]，S=[5]`。因此队首为 4、栈顶为 5。

---

## 相关链接

- 相关知识点：
