---
id: 202503-宁波十校-高三-月考-12
题型: 选择题
来源: "[[试卷/202503-宁波十校-高三-月考.pdf|202503-宁波十校-高三-月考.pdf]]"
试卷: 202503-宁波十校-高三-月考.pdf
年级: 高三
题号: "12"
难度: 困难
水平: 选考
知识点:
  - 09队列
  - 循环队列
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-31
图片核验: 无图片
tags:
  - 知识点/09队列
  - 知识点/循环队列
---

# 题目 12

12. 有如下 Python 程序段，下列说法不正确的是（   ）

```python
n = 6
m = 2
a = []
num = 0
que = [i + 1 for i in range(n)]
head = tail = 3                      # ①
while (head + 2) % n != tail:        # ②
    num += 1
    x = que[head]
    head = (head + 1) % n            # ③
    if num == m:
        num = 0
        m += 1
    else:
        que[tail] = x
        tail = (tail + 1) % n
                                        # ④
while head != tail:
    a = a + [que[head]]
    head = (head + 1) % n
```

- A. 执行该程序段后，`a` 列表中有两个元素，分别为 4 和 6
- B. 若将①处的语句修改为 `head=tail=0`，程序运行后，`a=[3,6]`
- C. 若将②处的循环表达式修改为 `(head+1)%n!=tail`，程序运行后 `a` 列表中只有 1 个元素
- D. 若将③处的语句 `head=(head+1)%n` 放到④处，不影响程序功能

---

## 答案

**正确答案：** A

---

## 解析

按原程序模拟，循环结束后 `a=[6,3]`，并不是 `[4,6]`，所以 A 不正确。B 改变了初始队首位置后输出 `[3,6]`；C 将保留元素数量改为 1；③移动队首的语句在每轮处理结束前执行即可，移到④处不改变本轮取值及入队逻辑。

---

## 相关链接

- 来源：[[试卷/202503-宁波十校-高三-月考.pdf|202503-宁波十校-高三-月考.pdf]]
