---
id: 202606-金华十校-高二-期末-15
题型: 填空题
来源: "[[试卷/202606-金华十校-高二-期末.pdf|202606-金华十校-高二-期末.pdf]]"
试卷: 202606-金华十校-高二-期末.pdf
年级: 高二
题号: "15"
难度: 困难
水平: 选考
知识点:
  - 09队列
  - 13查找与排序
  - 二分查找
  - 贪心算法
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-28
图片核验: 无图片
tags:
  - 知识点/09队列
  - 知识点/13查找与排序
  - 知识点/二分查找
  - 知识点/贪心算法
---

# 题目 15

15. 随着人工智能技术的飞速发展，数据中心的能耗成本优化成为了亟待解决的问题。为了尽可能降低整体运营支出，某数据中心需要为接下来的 `n` 个时段内完成一系列计算任务规划最优购电方案。具体规则如下：

① 第 `i` 个时段需要消耗 `w[i]` 单位电量，这部分电量必须在第 `i` 时段结束前准备好。  
② 可在任意时段购电，第 `i` 时段的实时电价为每单位 `c[i]` 元。  
③ 数据中心配有一块容量为 `k` 的储能电池。在电价较低的时段，可多买电存入电池，供后续使用。电池初始电量为 0，任何时刻电量必须满足 `0≤电量≤k`。

案例 1：时段数 `n=3`，电池容量 `k=5`，各时段用电需求 `w=[3,4,2]`，各时段实时电价 `c=[5,2,6]`，最少购电总花费为 27 元。具体方案如下：

- 第 1 个时段购买 3 单位电量，花费 3×5=15 元；这 3 单位电量全部用于当前时段的需求；
- 第 2 个时段购买 6 单位电量，花费 2×6=12 元；其中 4 单位立即使用，另外 2 单位存入电池；
- 第 3 个时段直接使用电池中剩余的 2 单位电量，无需额外购电；
- 总花费：15+12+0=27 元。

为实现最少花费，小张设计了每次都将电池充满，遇到更便宜的电价就“退掉”之前买贵的电，换成便宜的电的算法，请回答下列问题：

(1) 若将上述案例 1 的实时电价改为 `c=[1,2,6]`，则最少购电总花费为 ______ 元。

(2) 定义如下 `search(q,price)` 函数，`q` 列表每个元素由电价、电量构成，列表已按电价升序排序。函数功能是查找并返回最后一个单价小于 `price` 的元素索引，若未找到则返回 -1。

```python
def search(q, price):
    left, right = 0, len(q) - 1
    while left <= right:
        m = (left + right) // 2
        if price > q[m][0]:
            left = m + 1
        else:
            right = m - 1
    return right
```

① 若 `q=[[2,1],[3,3],[4,4],[5,2],[6,1],[8,3]]`，`price` 为 4，则返回值是 ______。  
② 调用 `search` 函数，若 `q` 列表的长度为 8，则 `while` 语句中循环体最多执行 ______ 次。

(3) 实现上述功能的 Python 程序如下，请在划线处填入合适的代码。

```python
# 读取时段数 n、电池容量 k，代码略
# 读取每时段用电需求存入列表 w 中，每时段电价存入列表 c 中，代码略
q = []
s = 0  # 记录已规划的总电量
ans = 0
for i in range(n):
    price, need = c[i], w[i]
    if len(q) > 0:
        pos = search(q, price)
        r = 0
        for j in range(pos + 1, len(q)):
            # 统计需要退订的“贵电”总和
            r += q[j][1]
        if r > 0:
            s -= r
            del q[pos + 1:]  # 删除 q 列表 pos+1 及之后的所有元素
    add = ____①____
    q.append([price, add])
    s = k + need
    while len(q) > 0 and need > 0:
        if q[0][1] > need:
            q[0][1] -= need
            s -= need
            ans += q[0][0] * need
            ____②____
        else:
            con = q[0][1]
            need -= con
            s -= con
            ____③____
            q.pop(0)  # 弹出队首元素
print(ans)
```

---

## 答案

**正确答案：** (1) 10；(2) ① 1，② 4；(3) ① `k+need-s`；② `need=0`；③ `ans+=q[0][0]*con`

---

## 解析

(1) 按最优购电策略模拟可得总花费为 10 元。(2) 单价小于 4 的最后一个元素是 `[3,3]`，索引为 1；长度为 8 的二分查找循环体最多执行 4 次。(3) 新购电量需要同时满足当前需求并把电池补至容量上限，即 `k+need-s`；当前队首电量足够时将 `need` 置 0；否则整档电量全部使用并按其单价计入总花费。

---

## 相关链接

- 相关知识点：
