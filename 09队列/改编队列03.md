---
id: 改编队列03
题型: 选择题
来源: "[[03python基础/202603-金丽衢-高三-月考-13|202603-金丽衢-高三-月考-13]]"
试卷: 改编出题
年级: 高三
题号: "03"
难度: 中等
知识点:
  - 09队列
  - 滑动窗口
  - 连续子序列
完成次数: 0
正确率:
状态: 未练习
错题原因:
创建日期: 2026-06-26
图片核验: 无图片
tags:
  - 状态/未练习
  - 知识点/09队列
  - 知识点/滑动窗口
---

# 改编队列03

数组 `a` 存储一列货物重量，卡车载重为 `weight`。要求选择一段连续货物，使总重不超过 `weight` 且尽可能大；若有多个总重相同的方案，选后出现的方案。如下程序中划线处代码依次应为（   ）

```python
a = [20,10,50,70,25,35,30,5,80]
weight = 100
i = j = s = 0
besti = bestj = bests = 0
while ______①______:
    s += a[j]
    j += 1
    while i <= j and s > weight:
        ______②______
        i += 1
    if ______③______:
        bests = s
        besti = i
        bestj = j
print(bests, besti, bestj)
```

- A. ① `j < len(a)`  ② `s -= a[i]`  ③ `s >= bests`  
- B. ① `i < len(a)`  ② `s -= a[j]`  ③ `s > bests`  
- C. ① `j <= len(a)`  ② `s += a[i]`  ③ `s >= weight`  
- D. ① `j < len(a)`  ② `i += 1`  ③ `s > weight`

---

## 答案

**正确答案：** A

---

## 解析

`j` 是右边界，循环条件为 `j < len(a)`。当窗口和超过载重时，从左侧移出 `a[i]` 并右移 `i`。题目要求总重相同取后出现方案，因此用 `s >= bests` 更新。

---

## 相关链接

- 改编自：[[03python基础/202603-金丽衢-高三-月考-13|202603-金丽衢-高三-月考-13]]
