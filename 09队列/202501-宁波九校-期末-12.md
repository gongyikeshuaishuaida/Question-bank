---
id: 202501-宁波九校-期末-12
题型: 选择题
来源: "[[试卷/202501-宁波九校-期末.pdf|202501-宁波九校-期末.pdf]]"
试卷: 202501-宁波九校-期末.pdf
题号: "12"
难度: 中等
知识点:
  - 09队列
  - 队列模拟
  - 03python基础
完成次数: 0
正确率:
状态: 未练习
错题原因:
创建日期: 2026-05-26
图片核验: 无图片
tags:
  - 状态/未练习
  - 知识点/09队列
---

# 题目 12

12. 有如下 Python 程序段：

```python
s = "ABCDEF"; head = 0; tail = 0
que = [""] * 10
for i in range(len(s)):
    if i % 2 == 0:
        que[tail] = s[len(s)-1-i]
    else:
        que[tail] = s[i-1]
    tail = tail + 1
for i in range(len(s)):
    print(que[head], end="")
    head = head+1
```

该程序运行后输出的结果为（   ）

- A. FAEBDC  
- B. FADCBE  
- C. FBDDBF  
- D. FDBACE

---

## 答案

**正确答案：** B

---

## 解析

循环中依次写入队列的字符为：`i=0` 写入 `F`，`i=1` 写入 `A`，`i=2` 写入 `D`，`i=3` 写入 `C`，`i=4` 写入 `B`，`i=5` 写入 `E`。随后从 `head=0` 开始顺序输出，结果为 `FADCBE`。

---

## 相关链接

- 来源：[[试卷/202501-宁波九校-期末.pdf|202501-宁波九校-期末.pdf]]
