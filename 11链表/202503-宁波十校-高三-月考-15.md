---
id: 202503-宁波十校-高三-月考-15
题型: 填空题
来源: "[[试卷/202503-宁波十校-高三-月考.pdf|202503-宁波十校-高三-月考.pdf]]"
试卷: 202503-宁波十校-高三-月考.pdf
年级: 高三
题号: "15"
难度: 困难
水平: 选考
知识点:
  - 11链表
  - 13查找与排序
  - 链表插入
  - 选择排序
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-31
图片核验: 已通过
tags:
  - 知识点/11链表
  - 知识点/13查找与排序
  - 知识点/链表插入
  - 知识点/选择排序
---

# 题目 15

15. 某校对高一新生进行平行分班，新生部分数据如第 15 题图 a 所示。具体分班规则如下：将高一新生按毕业学校进行分组，再对各校学生分别按总分降序排序，然后在此基础上按男生在前、女生在后按序进行蛇形分班。分成 5 个班的蛇形分班方法如第 15 题图 b 所示。编写程序模拟分班过程，生成并输出 `m` 个班的学生名单。

| 考号 | 姓名 | 学校 | 性别 | 成绩 |
| --- | --- | --- | --- | ---: |
| 20240338 | 邹*逸 | C校 | 女 | 461 |
| 20240628 | 祝*豪 | F校 | 女 | 378 |
| 20240227 | 朱*阳 | B校 | 男 | 420 |

![[attachments/202503宁波十校_15_图1.png]]

(1) 定义如下 `sort_score(a,st,ed)` 函数，`a` 列表每个元素的数据项依次为考号、姓名、学校、性别、成绩，列表已按学校为关键字升序排列。函数功能是将 `a` 列表中从 `a[st]` 到 `a[ed]` 的数据（包括 `a[st]` 和 `a[ed]`）按成绩降序排序。

```python
def sort_score(a, st, ed):
    i = st
    while i < ed:
        k = i
        for j in range(i + 1, ed + 1):
            if a[j][4] > a[k][4]:
                k = j
        if k != i:
            a[k], a[i] = a[i], a[k]  # 语句 A
        i += 1
```

① 调用 `sort_score` 函数，若 `a[st][4],a[st+1][4],…,a[ed][4]` 的值依次为 461、378、524、420、530，则语句 A 的执行次数是________。

② 修改程序中 `range(i + 1, ed + 1)` 的代码，使得函数功能保持不变。

(2) 定义如下 `sort_mf(data,head)` 函数，`data` 是一个已按各校成绩降序排序的链表。函数功能是将 `data` 中的数据保持原有相对位置不变的情况下按男生在前、女生在后的顺序排序。请在划线处填入合适的代码。

```python
def sort_mf(data, head):
    p = head
    while data[p][0][3] == "女":
        pre = p
        p = data[p][1]
    if p != head:
        data[pre][1] = data[p][1]
        data[p][1] = head
        head = p
    t = k = head
    while data[t][0][3] == "男":
        k = t
        t = data[t][1]
    while data[t][1] != -1:
        p = data[t][1]
        if data[p][0][3] == "男":
            ________________________
            data[p][1] = data[k][1]
            data[k][1] = p
            k = data[k][1]
        else:
            t = data[t][1]
    return head
```

(3) 实现平行分班的 Python 程序如下，请在划线处填入合适的代码。

```python
def proc(a, rs):
    s = 0
    for i in range(len(rs)):  # 分别对各校按成绩进行降序排序
        num = rs[i][1]
        ________________________①
        s += num

    data = [[a[0], -1]]
    head = 0
    for i in range(1, len(a)):
        data.append([a[i], data[i - 1][1]])
        data[i - 1][1] = i
    head = sort_mf(data, head)

    p = head
    bj = 0
    k = 1
    cla = [[] for i in range(m)]
    while p != -1:
        bj += k
        if bj > m:
            bj = m
            k = -1
        elif bj < 1:
            bj = 1
            k = 1
        data[p][0].append(bj)
        ________________________②
        p = data[p][1]
    return cla

# 输入需要分班的班级数 m，读取学生数据存入 a 列表；
# 对 a 列表按学校为关键字排序分组，并统计各校人数存入 rs，代码略
cla = proc(a, rs)
# 导出 1～m 班学生名单，代码略
```

---

## 答案

(1) ① 3；② `ed, i, -1`（即 `range(ed, i, -1)`）

(2) `data[t][1] = data[p][1]`

(3) ① `sort_score(a, s, s + num - 1)`；② `cla[bj - 1].append(data[p][0])`

---

## 解析

(1) 选择排序每轮寻找当前区间最大值。序列 461、378、524、420、530 的前三轮都发生交换，后续无需交换，共执行语句 A 3 次。候选位置从 `ed` 递减到 `i+1` 也能遍历同一范围，因此可改为 `range(ed,i,-1)`。

(2) 当 `p` 指向位于女生节点 `t` 后的男生节点时，要先令 `data[t][1]=data[p][1]` 将 `p` 从原位置摘下，再把它插入男生区末尾 `k` 之后，才能保持男女各自原有相对次序。

(3) `s` 是当前学校数据的起始下标，人数为 `num`，故排序区间为 `s` 到 `s+num-1`。蛇形计算得到班号 `bj` 后，应把当前学生记录加入下标为 `bj-1` 的班级列表。

---

## 相关链接

- 来源：[[试卷/202503-宁波十校-高三-月考.pdf|202503-宁波十校-高三-月考.pdf]]
