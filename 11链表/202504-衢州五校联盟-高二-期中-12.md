---
id: 202504-衢州五校联盟-高二-期中-12
题型: 选择题
来源: "[[试卷/202504-衢州五校联盟-高二-期中.pdf|202504-衢州五校联盟-高二-期中.pdf]]"
试卷: 202504-衢州五校联盟-高二-期中.pdf
年级: 高二
题号: "12"
难度: 困难
水平: 选考
知识点:
  - 11链表
  - 链表合并
  - 数组模拟链表
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-08-01
图片核验: 已通过
tags:
  - 知识点/11链表
---
# 题目 12

12.使用 `da` 和 `db` 模拟 2 个链表结构（节点数均大于1），每个节点包含数据区域和指针区域，各链表数据区域中的数值不重复且已从小到大排列。链表 `da` 头节点的值小于链表 `db` 头节点的值（如第 12 题图 a 所示）。现要将链表 `db` 中与 `da` 不重复的数据合并到链表 `da` 中，且保持升序排列（如第 12 题图 b 所示）。实现该功能的 python 程序如下，方框中应填入的正确代码为（   ）

![[attachments/202504衢州五校_12_图a图b.png]]

```python
ha=hb=0
q=ha; p=0
while hb!=-1 and p!=-1:
    p=da[q][1]
    if da[p][0]<db[hb][0]:
        q=p
        p=da[p][1]
    elif da[p][0]>db[hb][0]:
        da.append([db[hb][0],p])
        ____①____  # 此处是方框处
    else:
        hb=db[hb][1]
while hb!=-1:
    #将db 链表中剩余元素添加到da 链表中，代码略
```

- A. ① `da[q][1]=len(da)-1`；`hb=db[hb][1]`；`q=len(da)-1`
- B. ① `da[q][1]=p`；`hb=db[hb][1]`；`q=p`
- C. ① `da[p][1]=len(da)-1`；`hb=db[hb][1]`；`p=len(da)-1`
- D. ① `da[p][1]=q`；`hb=db[hb][1]`；`p=q`

---
## 答案

**正确答案：** A

---
## 解析

当 `da[p][0]` 大于当前 `db[hb][0]` 时，新节点追加到 `da` 末尾，其下标为 `len(da)-1`；应让当前节点 `da[q]` 指向新节点，再移动 `hb` 并把 `q` 更新为新节点下标。三行依次为 `da[q][1]=len(da)-1`、`hb=db[hb][1]`、`q=len(da)-1`，选 A。

---
## 相关链接

- 来源：[[试卷/202504-衢州五校联盟-高二-期中.pdf]]
