---
id: 202606-杭州-高二-期末-15
题型: 填空题
来源: "[[试卷/202606-杭州-高二-期末.pdf|202606-杭州-高二-期末.pdf]]"
试卷: 202606-杭州-高二-期末.pdf
年级: 高二
题号: "15"
难度: 困难
水平: 选考
知识点:
  - 11链表
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-22
图片核验: 无图片
tags:
  - 知识点/11链表
---
# 题目 15

15. 现要对长度为 $n$ 的序列 $a$ 进行加密, 加密结果存入序列 $c$ 中, 处理过程如下:

①从 a 中的第 1 个元素开始计数（从 1 开始），每数到第 k 个（1<k<n），将该元素和 a 中的第 1 个元素，依次移动至 c 的末尾，然后从该元素的下一个元素重新开始计数，继续这一过程，直至遍历完 a，完成一轮取数。重复以上操作，直到 a 中剩余元素个数小于 k 个。

② 若 $a$ 中还有剩余元素，则逆序移动至 $c$ 的尾部，结束加密过程。

如 a 为 $[1, 2, 3, 4, 5, 6, 7, 8]$ ，k 为 3，加密处理过程如第 15 题图所示，加密结果为 $[3, 1, 6, 2, 7, 4, 8, 5]$ 。

<table><tr><td>序列</td><td>初始值</td><td>第一轮取数</td><td>第二轮取数</td><td>剩余数</td></tr><tr><td>a</td><td>[1, 2, 3, 4, 5, 6, 7, 8]</td><td>[4, 5, 7, 8]</td><td>[5, 8]</td><td>[]</td></tr><tr><td>c</td><td>[]</td><td>[3, 1, 6, 2]</td><td>[3, 1, 6, 2, 7, 4]</td><td>[3, 1, 6, 2, 7, 4, 8, 5]</td></tr></table>

第 15 题图

(1) 若 a 为 [1,2,3,4], k 为 3, 按上述加密处理后, 加密结果为 ▲。

（2）定义如下函数 cal(n, k)，返回序列长度为 n、计数为 k 时，取数的总轮数。
```python
def cal(n, k):
    if n < k:
        return 0
    return cal(n - n // k * 2, k) + 1
```
①若 n 为 12，k 为 4，执行语句 t=cal(n, k) 后，变量 t 的值为 ▲。

②对于任意的 n 和 k，程序段 ▲（单选）执行后，变量 t 的值与调用函数 cal(n, k) 的返回值相同。
- A.

```python
t = 0
while n >= k:
    t += 1
    n = n - n // k * 2
```

- B.

```python
def convert(data, head, k, t):
    ①
    qcpre = -1
    for i in range(t):
        num = 0
        qa = qc
        while qa != -1:
            num += 1
            if ②:
                qapre = qa
                qa = data[qa][1]
            else:
                data[qapre][1] = data[qa][1]
                data[qa][1] = qc
                if qc == head:
                    head = qa
                else:
                    ③
                qcpre = qc
                qc = data[qc][1]
                qa = data[qapre][1]
                num = 0
    if qc != -1:
        # 将链表 data 中 qc 及其之后的节点逆序，代码略
        pass
    return head
```
(3) 实现加密功能的 Python 程序如下，请在划线处填入合适的代码。

’’读取待加密序列 a 存入 data 列表，并按原序列顺序建立链表，data 模拟链表结构，data[i]包含两个元素，data[i][0]为数据区域，data[i][1]为指针区域，head 为链表头指针；读取序列长度存入 n；设置 k，代码略’’
```python
t = cal(n, k)
head = convert(data, head, k, t)
# 输出链表 data 中的加密结果，代码略
```

---

## 答案

**正确答案：** (1) `[3,1,4,2]`；(2) ① 3；② A；(3) ① `qc = head`；② `num < k`；③ `data[qcpre][1] = qa`

---

## 解析

链表题需要维护头指针、前驱和后继关系，插入或删除节点后要同步更新指针域。

---

## 相关链接

- 相关知识点：
