---
id: 202605-浙东北联盟-高二-月考-15
题型: 填空题
来源: "[[试卷/202605-浙东北联盟-高二-月考.pdf|202605-浙东北联盟-高二-月考.pdf]]"
试卷: 202605-浙东北联盟-高二-月考.pdf
年级: 高二
题号: "15"
难度: 困难
水平: 选考
知识点:
  - 11链表
  - 链表插入
  - 贪心
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-30
图片核验: 已通过
tags:
  - 知识点/11链表
  - 知识点/链表插入
  - 知识点/贪心
---

# 题目 15

15. 某奶茶店配备两台相同的机器同时制作奶茶，每天接收大量订单。订单按下单顺序存储在列表`data`中，每份订单包含4个数据项：订单号、下单时间、制作时长和奶茶名称（假设每份订单仅包含一杯奶茶）。奶茶制作规则如下：

a. 订单只能在其下单时间之后开始制作，不能提前；  
b. 每台机器同一时间仅可制作一杯奶茶，且制作过程不能中断；  
c. 当某台机器空闲时，从所有“已到达但尚未制作”的订单中，优先选择制作时长最短的订单制作。

将下单后至开始制作前的时长定义为该订单的等待时长。在满足所有订单制作的前提下，实现订单等待总时长最短，计算总等待时长，并输出。（注：本题中所有时间均以分钟为单位）

现有4份订单，数据如下：`data = [[101,10,5,"珍珠奶茶"],[102,12,4,"椰果奶茶"],[103,12,6,"红豆奶茶"],[104,15,3,"柠檬奶茶"]]`，订单制作顺序及总等待时长如第15题图所示。

![[attachments/202605浙东北联盟高二月考_15_图1.png]]

请回答下列问题。

(1) 若将上例中“椰果奶茶”的制作时间调整为7分钟，则总等待时长为____分钟。

(2) 定义如下函数`lnk(wait_lnk, head, idx)`，函数功能为将新节点按制作时长升序插入链表。

```python
def lnk(wait_lnk, head, idx):
    if head == -1:
        head = idx
        return head
    new_dur = wait_lnk[idx][1]
    p = q = head
    while wait_lnk[p][1] > new_dur:  # ①
        q = p
        p = wait_lnk[p][4]
    if p == head:
        ____________________  # ②
        head = idx
    else:
        wait_lnk[idx][4] = p
        wait_lnk[q][4] = idx
    return head
```

① 程序加框处代码有错，请改正。  
② 请在划线处填入合适代码。

(3) 实现以上功能的Python程序如下，请在划线处填入合适代码。

```python
'''订单信息按下单顺序存入data列表，每个元素包含4个数据项，依次为订单号、
下单时间、制作时长和奶茶名称，代码略。'''
head = -1
m = [0, 0]
wait_lnk = []
total_wait = 0
i = 0
while i < len(data) or head != -1:
    k = 0
    if m[0] > m[1]:
        k = 1
    while i < len(data) and data[i][1] <= m[k]:
        num, time, dur, name = data[i]
        idx = len(wait_lnk)
        wait_lnk.append([num, dur, time, name, -1])
        head = lnk(wait_lnk, head, idx)
        i += 1
    if head != -1:
        num, dur, time, name, head = wait_lnk[head]
        start_time = m[k]
        ____________________  # ①
        total_wait += wait
        m[k] = start_time + dur
        # 输出订单制作信息，代码略
    else:
        if i < len(data):
            ____________________  # ②
# 输出总等待时长，代码略
```

---

## 答案

(1) `6`

(2) ① `p != -1 and wait_lnk[p][1] <= new_dur`；② `wait_lnk[idx][4] = head`

(3) ① `wait = start_time - time`；② `m[k] = data[i][1]`

---

## 解析

(1) 两台机器依次在10、12分钟开始制作101和103；15分钟制作104；18分钟才开始制作102，订单102等待`18-12=6`分钟，其余订单等待0分钟。

(2) 链表按制作时长升序排列，应在当前节点时长不大于新节点时继续向后查找，并先判断`p != -1`避免访问空指针。若新节点插到表头，其后继应指向原`head`。

(3) 等待时长等于实际开始时间减去下单时间。没有待制作订单时，只需把当前选中的空闲机器`m[k]`推进到下一订单的到达时间，下一轮即可接收该订单。

---

## 相关链接

- 类似知识点题：[[11链表/202405-北斗星盟-高二-月考-15]]、[[11链表/202501-台州-高二-期末-15]]、[[11链表/202505-北斗星盟-高二-月考-15]]、[[11链表/202506-宁波九校-高二-期末-15]]
- 来源：[[试卷/202605-浙东北联盟-高二-月考.pdf|202605-浙东北联盟-高二-月考.pdf]]
