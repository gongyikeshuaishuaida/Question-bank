---
id: 202606-舟山-高二-期末-15
题型: 填空题
来源: "[[试卷/202606-舟山-高二-期末.pdf|202606-舟山-高二-期末.pdf]]"
试卷: 202606-舟山-高二-期末.pdf
年级: 高二
题号: "15"
难度: 困难
水平: 选考
知识点:
  - 11链表
  - 03python基础
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-22
图片核验: 已通过
tags:
  - 知识点/11链表
  - 知识点/03python基础
---
# 题目 15

15. 该校餐饮中心继续利用该系统对后厨备餐效率进行跟踪分析。后厨在备餐过程中一般会遵循以下策略：①厨师按照订餐数据中的到达时间先后顺序依次备餐；②为保证烹饪过程的连贯，每种餐品都由固定的一组厨师进行烹饪；③每个厨师每次可以同时烹饪 k 份相同类型的餐品；④每次安排最早结束的厨师进行烹饪任务，如存在多个厨师则选择编号最小的厨师。

（1）订餐数据里到达时间前 20 的数据如第 15 题图 a 所示，后厨人员配置及餐品制作时间如第 15 题图 b 所示，若后厨统一从 11:30 开始备餐，`k=3`，则唐蕊同学订的炒面实际完成的时间是______。

![[attachments/202606舟山高二_15_图1.png]]

![[attachments/202606舟山高二_15_图2.png]]

（2）定义如下自定义函数 `insert_node(h, index)`，参数 `h` 是列表 `a` 中某一段链表的头指针，该链表已按到达时间进行升序排列。函数的功能是将 `index` 节点插入到链表中，并保持升序排列，同时返回新的 `h`。

```python
def insert_node(h, index):
    q = p = h
    while a[p][0] <= a[index][0]:       #1
        q = p                           #2
        p = a[p][2]                     #3
    if p == h:
        h = index
    else:
        a[q][2] = index                 #4
        a[index][2] = p                 #5
    return h
```

该程序存在两处逻辑错误，导致功能无法实现，存在错误的语句编号是______。

（3）为确保每位师生到达食堂后都能及时领到所订的餐品，后厨需要提前进行备餐。编写 Python 程序，读取当日订餐数据和一次可以同时烹饪的份数 `k`，输出每种餐品的各备餐量及需要提前多少时间开始备餐。程序运行结果如第 15 题图 c 所示。

![[attachments/202606舟山高二_15_图3.png]]

部分程序代码如下，请在划线处填入合适的代码。

```python
def find_cooker(next_time):
    # 在 next_time 列表中寻找最早结束当前烹饪任务的厨师，并返回该厨师索引，代码略

a = []
hlist = {"牛肉面": -1, "盖浇饭": -1, "炒面": -1, "炒饭": -1}
cook_time = {"牛肉面": 8, "盖浇饭": 10, "炒面": 8, "炒饭": 6}
cookers_count = {"牛肉面": 2, "盖浇饭": 3, "炒面": 3, "炒饭": 2}

# 从 data.txt 文件中读取订餐数据，经过处理后依次添加到列表 a，
# a 中每个元素包含到达时间、餐品名称和指针，代码略。

for i in range(len(a)):
    ______①______

for d in hlist:
    next_time = [0] * ______②______
    cnt = 0
    p = hlist[d]
    while p != -1:
        cnt += 1
        if ______③______:
            c = find_cooker(next_time)
            next_time[c] = max(next_time[c], a[p][0]) + cook_time[d]
        p = a[p][2]
```

---

## 答案

**正确答案：** （1）11:38；（2）1,5；（3）① `hlist[a[i][1]] = insert_node(hlist[a[i][1]], i)`；② `cookers_count[d]`；③ `cnt % k == 0`

---

## 解析

依据题干中的关键概念、数据表示、系统组成或程序执行过程判断，正确答案与题意一致。

---

## 相关链接

- 相关知识点：
