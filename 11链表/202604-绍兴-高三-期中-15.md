---
id: 202604-绍兴-高三-期中-15
题型: 填空题
来源: "[[试卷/202604-绍兴-高三-期中.pdf|202604-绍兴-高三-期中.pdf]]"
试卷: 202604-绍兴-高三-期中.pdf
年级: 高三
题号: "15"
难度: 较难
知识点:
  - 11链表
  - 链表
  - 排序
  - 调度算法
完成次数: 0
正确率:
状态: 未练习
错题原因:
创建日期: 2026-06-25
图片核验: 已通过
tags:
  - 状态/未练习
  - 知识点/11链表
  - 知识点/链表
  - 知识点/排序
  - 知识点/调度算法
---

# 题目 15

15. 现有 m 台 A 型机器（编号 0~m-1），n 台 B 型机器（编号 m~m+n-1），需处理当天提交的 w 项作业。每项作业包含开始时间、结束时间、作业类型（分为 A、B 两类）。初始时，各台机器均处于空闲状态。编写程序，对作业按开始时间排序后进行逐项分配，规则如下：

① A 类作业可分配至 A 型或 B 型机器，B 类作业只能分配至 B 型机器。  
② 每次分配时，优先选择最先结束先前作业的机器；若有多台机器同时满足时，则优先选择编号最小的。  
③ 若选中的机器当前尚未空闲，作业将被延期，直到该机器空闲后立即执行；作业所需完成时长（结束时间-开始时间）保持不变。

![[attachments/202604绍兴高三_15_图1.png]]

（1）若 m 为 1，n 为 1，各项作业如第 15 题图所示，所有作业处理完毕的结束时间为____。

（2）对作业按开始时间排序的部分 Python 程序如下：

```python
def simplesort(a):
    cnt = 60 * 24
    b = [0] * (cnt + 1)
    w = len(a)
    temp = [0] * w
    for i in range(w):
        s = a[i][0]
        b[s] += 1
    for i in range(1, cnt + 1):
        b[i] += b[i - 1]
    for i in range(w):
        s = a[i][0]
        b[s] -= 1
        temp[b[s]] = a[i]
    return temp
```

① 若 w 为 10000，程序执行加框处 `for` 语句后，b 数组中元素的最大值为____。  
② 调试程序时，发现划线处语句有误，应当修改为____。

（3）模拟作业分配功能的部分 Python 程序如下，请在划线处填入合适的代码。

```python
def allocate(data, m, n):
    seatcount = m + n
    seats = []
    for i in range(seatcount):
        seats.append([-1, i + 1])
    seats[seatcount - 1][1] = -1
    head = 0
    for d in data:
        start, end, ptype = d[0], d[1], d[2]
        duration = end - start
        p = head
        pre = idx = -1
        while p != -1:
            if ptype == "A" or ______①______:
                idx = p
                break
            pre = p
            p = seats[p][1]
        if pre == -1:
            head = seats[idx][1]
        else:
            seats[pre][1] = seats[idx][1]
        if seats[idx][0] <= start:
            seats[idx][0] = end
        else:
            ______②______
        pre, p = -1, head
        while p != -1:
            v, k = seats[p][0], seats[idx][0]
            if v < k or (v == k and p < idx):
                pre = p
                p = seats[p][1]
            else:
                break
        if pre == -1:
            head = idx
        else:
            seats[pre][1] = idx
        ______③______
    # 计算所有作业处理完毕的最终时间，存入 maxt，代码略
    return maxt
```

---

## 答案

**正确答案：** （1）16:30；（2）① 10000；② `for i in range(w - 1, -1, -1)`；（3）① `p >= m`；② `seats[idx][0] += duration`；③ `seats[idx][1] = p`。

---

## 解析

A 类作业可使用任意机器，B 类作业只能使用 B 型机器。机器链表按当前结束时间和编号维护，分配时先找到符合类型且最早空闲的机器，取出后更新结束时间，再按结束时间和编号重新插回有序链表。计数排序逆向遍历可保持相同开始时间作业的原相对顺序。

---

## 相关链接

- 来源：[[试卷/202604-绍兴-高三-期中.pdf|202604-绍兴-高三-期中.pdf]]
