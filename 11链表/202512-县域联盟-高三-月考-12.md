---
id: 202512-县域联盟-高三-月考-12
题型: 选择题
来源: "[[试卷/202512-县域联盟-高三-月考.pdf|202512-县域联盟-高三-月考.pdf]]"
试卷: 202512-县域联盟-高三-月考.pdf
年级: 高三
题号: "12"
难度: 困难
水平: 选考
知识点:
  - 11链表
  - 链表重排
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-01
图片核验: 已通过
tags:
  - 知识点/11链表
  - 知识点/链表重排
---

# 题目 12

12. 使用列表 `d` 模拟链表结构（节点数 `n>0` 且 `n` 为偶数），每个节点含数据区域和指针区域，`head` 为头指针。按数据区域首字母分为 A 类、B 类节点。要将链表从如图 a 所示“ABAB...”的顺序，调整为如图 b 所示“BABA...”的顺序。实现上述功能 Python 程序段如下：

```python
p = head
head = d[p][1]
while p != -1:
    q = d[p][1]
    r = d[q][1]
    d[q][1] = p
    # 方框处
```

![[attachments/202512县域联盟高三月考_12_图1.png]]

方框中应填入的正确代码为（   ）

- A.

```python
if d[q][1] == -1:
    d[p][1] = -1
else:
    d[r][1] = d[p][1]
p = d[p][1]
```

- B.

```python
if r == -1:
    d[p][1] = -1
else:
    d[p][1] = d[r][1]
p = r
```

- C.

```python
if d[q][1] == -1:
    d[p][1] = -1
else:
    d[r][1] = d[p][1]
p = r
```

- D.

```python
if r == -1:
    d[p][1] = -1
else:
    d[p][1] = d[r][1]
p = d[p][1]
```

---

## 答案

**正确答案：** B

---

## 解析

每次处理一组 A、B 节点，先让 B 指向当前 A。若 B 后面没有后续节点，则当前 A 指针置为 -1；否则当前 A 应指向下一组中的 B 节点，即 `d[r][1]`，然后 `p = r` 进入下一组。

---

## 相关链接

- 类似知识点题：[[11链表/202604-杭州-高三-二模-12]]
- 来源：[[试卷/202512-县域联盟-高三-月考.pdf|202512-县域联盟-高三-月考.pdf]]
