---
id: 202507-金华十校-高二-期末-12
题型: 选择题
来源: "[[试卷/202507-金华十校-高二-期末.pdf|202507-金华十校-高二-期末.pdf]]"
试卷: 202507-金华十校-高二-期末.pdf
年级: 高二
题号: "12"
难度: 中等
水平: 选考
知识点:
  - 11链表
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-04
图片核验: 无图片
tags:
  - 知识点/11链表
---

# 题目 12

12. 有如下 Python 程序段：

```python
link = [[1, 3], [3, 2], [7, -1], [5, 1], [4, 3]]
head = 0; n = 2
length = 1
curr = head
while link[curr][1] != -1:
    length += 1
    curr = link[curr][1]
index = head
count = 0
target_index = length - n - 1
if target_index < 0:
    head = link[head][1]
else:
    while count < target_index:
        count += 1
        index = link[index][1]
    link[index][1] = link[link[index][1]][1]
print(link)
```

执行该程序后，输出的结果是（   ）

- A. `[[1, 3], [3, 2], [7, -1], [5, 1], [4, 3]]`
- B. `[[1, 3], [3, 2], [7, -1], [5, 2], [4, 3]]`
- C. `[[1, 1], [3, 2], [7, -1], [5, 1], [4, 3]]`
- D. `[[1, 3], [3, -1], [7, -1], [5, 1], [4, 3]]`

---

## 答案

**正确答案：** B

---

## 解析

链表模拟：每个节点 `[data, next_index]`。

遍历链表得到顺序：`0 → 3 → 1 → 2 → -1`，对应值 `1 → 5 → 3 → 7`，链表长度 `length = 4`。

`n = 2`，`target_index = 4 - 2 - 1 = 1`，即要删除倒数第 2 个节点（正数第 3 个节点，索引 1）。

`target_index >= 0`，进入 else 分支：
- `count = 0`，`index = 0`
- `count < 1`：`count = 1`，`index = link[0][1] = 3`
- 循环结束，`index = 3`

执行删除：`link[3][1] = link[link[3][1]][1] = link[1][1] = 2`

即将索引 3 的 next 从 1 改为 2，跳过了索引 1 对应的节点（值为 3）。

修改后的 `link = [[1, 3], [3, 2], [7, -1], [5, 2], [4, 3]]`。

---

## 相关链接

- 相关知识点：
