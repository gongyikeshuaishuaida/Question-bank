---
id: 202506-台州-高二-期末-15
题型: 非选择题
来源: "[[试卷/202506-台州-高二-期末.pdf|202506-台州-高二-期末.pdf]]"
试卷: 202506-台州-高二-期末.pdf
年级: 高二
水平: 期末
题号: "15"
难度: 困难
知识点: [08数组, 13查找与排序]
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-08-02
图片核验: 无图片
tags: [知识点/08数组, 知识点/13查找与排序]
---
# 题目 15

某工厂有 `n` 种零件需要加工，每种零件的信息包含编号、加工难度、收益、数量。有 `m` 个工人报名参与加工，每个工人的信息包含技能等级、报名时间。加工难度和技能等级分 9 个级别，分别用 1～9 的整数表示。每个工人按照报名时间依序选择一个收益最高的零件加工，所选零件的加工难度不超过其技能等级。

编写程序模拟零件加工分配过程，先将零件数据按加工难度分类整理，然后每个工人按报名时间依序选择零件加工，计算所有工人能够获得的最高总收益。

第15题图 a 零件数据：

| 编号 | 加工难度 | 收益 | 数量 |
|---:|---:|---:|---:|
| 1 | 4 | 14 | 1 |
| 2 | 4 | 10 | 1 |
| 3 | 6 | 5 | 4 |
| 4 | 2 | 12 | 1 |
| 5 | 7 | 11 | 2 |
| 6 | 7 | 12 | 1 |

第15题图 b 工人数据：

| 技能等级 | 报名时间 |
|---:|---|
| 8 | 8:30 |
| 4 | 7:30 |
| 5 | 9:00 |
| 6 | 10:00 |

（1）如果工厂有 6 种零件，如图 a 所示，有 4 个工人报名参与加工，如图 b 所示，则能够获得的最高总收益为 **48**。

（2）定义如下 `sort_work(data, st, ed)` 函数，列表 `data` 存储零件数据，每个元素包含编号、加工难度、收益、数量 4 个数据项。函数功能是将 `data[st]`～`data[ed]` 中的零件数据按“收益”降序排序。

```python
def sort_work(data, st, ed):
    n = ed - st + 1
    for i in range(n - 1):
        for j in range(ed, st + i, -1):  # ①
            if data[j][2] > data[j - 1][2]:  # ②；此处是方框处
                data[j], data[j - 1] = data[j - 1], data[j]
    return data
```

①划线处应填入的代码为 **B：`ed, st+i, -1`**；②加框处应改为 **`data[j][2] > data[j-1][2]`**（写 `>=` 亦可）。

（3）实现上述功能的部分 Python 程序如下。已知 `data` 已按加工难度升序排序，`wk` 已按报名时间升序排序。

```python
h = [0] * 10  # h[i] 存储“加工难度”为 i 的起始位置
t = [0] * 10
c = [0] * 10
for i in range(n):
    k = data[i][1]  # ①
    c[k] += 1
for i in range(1, 10):
    h[i] = h[i - 1] + c[i - 1]
    t[i] = h[i] + c[i] - 1
    if h[i] <= t[i]:
        data = sort_work(data, h[i], t[i])
ans = 0
for w in wk:
    mx, p = 0, -1
    for i in range(w[0], 0, -1):
        if h[i] <= t[i] and data[h[i]][2] > mx:  # ②
            mx = data[h[i]][2]
            p = i
    if p == -1:  # 该工人无法分配工作
        continue
    ans += mx
    data[h[p]][3] -= 1  # ③
    if data[h[p]][3] == 0:
        h[p] += 1
print("最高总收益为：", ans)
```

① **`k = data[i][1]`**；② **`h[i] <= t[i] and data[h[i]][2] > mx`**；③ **`data[h[p]][3] -= 1`**。

---
## 答案

（1）48；（2）① B（`ed,st+i,-1`）；② `data[j][2] > data[j-1][2]`（或 `>=`）；（3）① `k=data[i][1]`；② `h[i]<=t[i] and data[h[i]][2]>mx`；③ `data[h[p]][3]-=1`。

---
## 解析

工人按报名时间处理：技能等级 4 的工人先选收益 14 的 4 级零件；技能等级 8 的工人选收益 12 的 7 级零件；技能等级 5 的工人选收益 12 的 2 级零件；技能等级 6 的工人再选收益 10 的 4 级零件，总收益为 48。程序先按难度建立每组的首尾下标，再在每组内按收益降序排列；分配后扣减当前零件数量，数量为 0 时移动该难度组的起始位置。

---
## 相关链接

- [[04数据处理/202506-台州-高二-期末-14]]
- [[13查找与排序/202506-台州-高二-期末-10]]
