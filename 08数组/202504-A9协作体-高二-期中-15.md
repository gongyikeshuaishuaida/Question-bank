---
id: 202504-A9协作体-高二-期中-15
题型: 非选择题
来源: "[[试卷/202504-A9协作体-高二-期中.pdf|202504-A9协作体-高二-期中.pdf]]"
试卷: 202504-A9协作体-高二-期中.pdf
年级: 高二
题号: "15"
难度: 困难
水平: 选考
知识点: [08数组, 加密解密, 矩阵]
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-08-01
图片核验: 已通过
tags: [知识点/08数组]
---
# 题目 15

某字符串仅由大写字母组成，其加密方法描述如下：

① 将待加密字符依次转换为数字，“A”对应0，“B”对应1，以此类推；

② 将转换后的数字按列优先存储的方式，存入 n*n 的矩阵中，空缺部分用-1补全；

③ 值变换：将矩阵中每一行除-1外的数据增加 k 值，若增加后的值超过25，则重新从0开始；

④ 位置变换：将矩阵中每一行的数据向左循环移动 k 位；

⑤ 按行优先依次读取矩阵中的数字；

⑥ 将0～25按字母表顺序转换为字母，-1用“#”代替，输出加密后的字符串。

例如，明文为“XYZ”，`n=2`、`k=1` 时，加密过程如下图所示。

![[attachments/202504A9高二_15_示例变换.png]]

（1）若 `n=2`、`k=2`，明文为“ABC”，则密文为 ______。

（2）实现上述功能的解密程序如下，请在划线处填入合适的代码。

```python
def decrypt_yw(array,k):
    # 循环右移 k 位，代码略
    pass
s=input("请输入密文：")
n=int(input("n="))
k=int(input("k="))
a=[[-1 for i in range(n)] for i in range(n)]
for i in range(len(s)):
    if ____①____:
        a[i//n][i%n]=ord(s[i])-ord("A")
for i in ____②____:
    a[i]=decrypt_yw(a[i],k)
    for j in range(n):
        if a[i][j]!=-1:
            a[i][j]=a[i][j]-k
mw=""
for j in range(n):
    for i in range(n):
        if a[i][j]!=-1:
            mw=____③____  # 此处是方框处
print("明文为：",mw)
```

（3）程序加框处代码有错，请改正。

```python
a[i][j]=a[i][j]-k
```

---
## 答案

**参考答案：** （1）CED#；（2）① s[i]!="#"；② range(n)；③ mw+chr(a[i][j]+ord("A"))；（3）a[i][j]=(a[i][j]-k+26)%26。

---
## 解析

ABC 的矩阵变换结果按行读取为 CED#。解密需跳过“#”，逐行右移，按列读取并转换成字母；减 k 后用模26避免出现负数。
