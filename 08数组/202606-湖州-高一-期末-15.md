---
id: 202606-湖州-高一-期末-15
题型: 填空题
来源: "[[试卷/202606-湖州-高一-期末.pdf|202606-湖州-高一-期末.pdf]]"
试卷: 202606-湖州-高一-期末.pdf
年级: 高一
题号: "15"
难度: 困难
水平: 选考
知识点:
  - 08数组
  - 字典
  - 文件读取
  - 组合枚举
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-28
图片核验: 无图片
tags:
  - 知识点/08数组
  - 知识点/字典
  - 知识点/文件读取
  - 知识点/组合枚举
---

# 题目 15

15. 学校食堂在统计学生消费金额的同时，还需要找出哪些菜品经常被同时购买，从而更好地推出更受欢迎的套餐。食堂收集了近期学生的就餐数据，将流水号及菜品数据存入文件 `"data.txt"`，部分数据如下：

```text
202605030001,x4,x6
202605080002,x4,x5,x2,x6
202605040003,x4,x6,x5
202605070004,x3,x5,x2,x4
202605090005,x2,x1,x5,x6
202605070006,x4,x5,x2,x1
202605030007,x5,x3,x2,x1
202605030008,x3,x6,x5,x4
```

数据格式每行为“流水号+菜品名”，每项用英文逗号隔开，菜品名由“x”+编号组成（菜品编号小于 10）。程序统计所有不同“菜品对”的关联次数，即两个不同菜品同时出现在同一条流水记录中表示关联一次，找出关联次数最多的“菜品对”，若有多组则全部输出。以上数据的输出结果为：

```text
关联次数最多的菜品对：x4和x5 次数：5
关联次数最多的菜品对：x2和x5 次数：5
```

(1) 定义如下 `read` 函数，读取指定文件，对每一行数据进行预处理，提取出所有菜品编号，返回列表 `li`，请在划线处填上合适的代码。

```python
def read(file):
    f = open(file, "r")  # 读取文件
    li = []
    for line in f:
        s = ""
        __________
        for i in range(len(line.strip())):
            # strip() 函数删除字符串末尾的 "\n"
            ch = line[i]
            if ch == ",":
                flag = True
            if ch >= "0" and ch <= "9" and flag:
                s = s + ch
        li.append(s)
    f.close()
    return li
```

(2) 定义如下 `get_pairs` 函数，其功能是生成菜品关联列表，其中每个元素是两个菜品编号按从小到大顺序拼接的字符串。

```python
def get_pairs(st):
    pairs = []
    ____①____
    for i in range(n-1):
        for j in range(i+1, n):
            if st[i] > st[j]:
                pairs.append(st[j] + "-" + st[i])
            else:
                pairs.append(st[i] + "-" + st[j])
    return pairs
```

① 请在划线处填上合适代码。  
② 加框处代码如果修改成 `for j in range(0,n)`，程序的运行结果是否有影响？______（选填：是或否）

(3) 实现上述功能的主程序如下，请在划线处填入合适的代码。

```python
dic = {}
data = read("data.txt")
for i in data:
    for j in get_pairs(i):
        if j in dic:
            ____①____
        else:
            dic[j] = 1

maxc = 0
for i in dic:
    if dic[i] > maxc:
        maxc = dic[i]
for j in dic:
    if ____②____:
        a, b = j.split("-")  # 以“-”为分隔符将字符串进行拆分
        print("关联次数最多的菜品对：", "x"+a+"和"+"x"+b, "次数：", maxc)
```

---

## 答案

**正确答案：** (1) `flag=False`；(2) ① `n=len(st)`；② 是；(3) ① `dic[j]+=1`；② `dic[j]==maxc`

---

## 解析

(1) 在读到第一个逗号前不应提取流水号中的数字，因此先令 `flag=False`，读到逗号后再置为 `True`。(2) `n` 是菜品编号字符串的长度；若内层循环从 0 开始，会生成自身配对和重复配对，结果会发生变化。(3) 已存在的菜品对需要把计数加 1；最后输出所有出现次数等于最大值 `maxc` 的菜品对。

---

## 相关链接

- 相关知识点：
