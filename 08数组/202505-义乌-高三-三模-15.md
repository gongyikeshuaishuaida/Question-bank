---
id: 202505-义乌-高三-三模-15
题型: 非选择题
来源: "[[试卷/202505-义乌-高三-三模.pdf|202505-义乌-高三-三模.pdf]]"
试卷: 202505-义乌-高三-三模.pdf
年级: 高三
题号: "15"
难度: 较难
水平: 选考
知识点:
  - 08数组
  - 13查找与排序
  - 二分查找
  - 区间划分
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-08-01
图片核验: 已通过
tags:
  - 知识点/08数组
---
# 题目 15

15. 有一批 `n` 个零件需要加工，编号为 0 到 `n - 1`，编号为 `i` 的零件的加工难度为 `a[i]`。工厂有 `m` 台机器，将所有零件分成 `m` 个编号连续的区间，每台机器依次加工一个区间内的零件。为了提高加工效率，按以下规则划分区间：①从左到右划分这 `m` 个区间，每个区间的零件数量至少 1 个，不多于 `k` 个。②一个区间内的零件加工难度的最大值与最小值的差距为该区间的“极差”。③这 `m` 个区间的“极差”的最大值称为 DIF 值，划分时应使 DIF 值尽量最小。现在要求计算加工这批零件的最小 DIF 值，并输出一种可行的区间划分方案。例如，有 8 个零件，加工难度 `a=[5,4,2,6,2,8,7,3]`，有 2 台机器，每台机器加工零件的数量不多于 5 个。一种划分方案为：第 1 组的难度为 `[5,4,2,6,2]`，极差为 4，第 2 组的难度为 `[8,7,3]`，极差为 5，则该分组方案的 DIF 值为 5。在这个例子中，5是最小的 DIF 值。请回答下列问题：

![[attachments/202505义乌_15_运行示例.jpg]]

（1）若在题目例子中增加 1 台机器，其他参数不变，则最小的 DIF 值是 ________。

（2）定义 `bigsma(a)` 函数，预处理出任意一个连续区间内，加工难度的最大值和最小值。

```python
def bigsma(a):
    # 遍历所有可能的区间，求每个区间最大值
    for i in range(n):
        for j in range(i, n):
            if i == j:
                max_vals[i][j] = a[i]
            else:
                for k in range(i, j + 1):
                    max_vals[i][j] = max(max_vals[i][j], a[k])
    # 遍历所有可能的区间，求每个区间最小值
    for i in range(n):
        for j in range(i, n):
            if i == j:
                min_vals[i][j] = a[i]
            else:
                min_vals[i][j] = min(min_vals[i][j - 1], a[j])
    return max_vals, min_vals
```

该函数中求区间最（①）值（填：大或小）的算法效果更高，其时间复杂度为 O（②）。

（3）定义 `judege(x)` 函数，用于判断 DIF 值为 `x` 时，是否可以找到一种可行的区间划分方案并记录方案。实现该功能的 Python 代码如下，请在程序中划线处填入合适的代码。

```python
def judge(x):
    cnt = start = 0
    ans = []
    while start <= n - 1:
        end = start
        for j in range(start, start + k):
            if j > n - 1 or j > n - (m - cnt):
                break
            cur_max = max_vals[start][j]
            cur_min = min_vals[start][j]
            if ①:
                break
            else:
                end = j
        ans.append([start, end])
        cnt += 1
        ②
        if cnt == m:
            return True, ans
    return False, []
```

①、②处填：① `cur_max - cur_min > x`；② `start = end + 1`。

（4）算法思想：由于 DIF 越小，分组成功的可能性越小，反之越大，答案具有单调性。故使用二分算法查找答案并判定，求出最小的 DIF 的值。

```python
#读取零件数量存入 n，机器数量存入 m，每个区间最多零件数存入 k，保证 k> n//m
#每个零件的加工难度存入列表 a，代码略。

max_vals = [[0] * n for i in range(n)]# 初始化存储每个区间最大值的二维数组
min_vals = [[0] * n for i in range(n)]# 初始化存储每个区间最小值的二维数组
max_vals, min_vals = bigsma(a)
left = 0; right = max(a) - min(a)
final_ans = []
while left <= right:
    mid = (left + right) // 2
    ok, ans = judge(mid)
    if ok:
        answer = mid
        final_ans = ans
        right = mid - 1
    else:
        left = mid + 1
print("DIF 值为:", answer)
print("一种可行的分组方案：")
for x in final_ans:
    print(a[x[0] : x[1] + 1])
```

```txt
输入：
8 2 5
[5, 4, 2, 6, 2, 8, 7, 3]
输出：
DIF 值为：5
一种可行的分组方案：
[5, 4, 2, 6, 2]
[8, 7, 3]
```

---
## 答案

**参考答案：**

- （1）4
- （2）① 小；② `n^2`
- （3）① `cur_max - cur_min > x`；② `start = end + 1`
- （4）`final_ans = ans`

---
## 解析

增加一台机器后可以将示例序列划分为 `[5,4]`、`[2,6,2]`、`[8,7,3]` 等三个连续区间，三个区间极差最大值可降为4。求区间最大值的程序对每个区间还要遍历内部所有元素，复杂度为 O（`n^3`）；求区间最小值时利用前一段的最小值递推，复杂度为 O（`n^2`），因此（2）应填“小”和 `n^2`。判定时若当前区间极差超过 `x` 就停止扩展，完成一组后令 `start=end+1`；二分成功时保存当前方案 `final_ans = ans`，并输出最终 DIF 和区间。

---
## 相关链接

- 来源：[[试卷/202505-义乌-高三-三模.pdf]]
