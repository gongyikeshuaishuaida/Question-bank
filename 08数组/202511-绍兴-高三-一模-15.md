---
id: 202511-绍兴-高三-一模-15
题型: 填空题
来源: "[[试卷/202511-绍兴-高三-一模.pdf|202511-绍兴-高三-一模.pdf]]"
试卷: 202511-绍兴-高三-一模.pdf
年级: 高三
题号: "15"
难度: 困难
水平: 选考
知识点:
  - 08数组
  - 二分查找
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-02
图片核验: 已通过
tags:
  - 知识点/08数组
  - 知识点/二分查找
---

# 题目 15

某公司使用一辆智能无人车来完成多件货物的运载任务（单件货物不拆分运输）。无人车每天单向行驶一次，自起点依次经过装卸点 1，装卸点 2，…，装卸点 n。每件货物的运载任务包括货物编号、货物重量（整数）、装货点和卸货点，如第 15 题图所示：

![[attachments/202511绍兴_15_图1.png]]

规定：每件货物的卸货点编号 > 装货点编号。请回答下列问题：

(1) 若一辆空车要当天完成如第 15 题图所示的运载任务，则该车的最小载重量为____（填数字）。

(2) 若公司要在 D 天内将多件货物从同一装货点运送到各个目标卸货点。要求无人车按货物编号顺序依次完成所有货物运输，求该车的最小载重量。实现上述功能的 Python 程序如下：

```python
def canShip(weights, D, C):
    days = 1
    cur = 0
    for w in weights:
        if cur + w <= C:
            cur += w
        else:
            days += 1
            cur = w
            if days > D:
                return False
    return True

# 非空列表 weights 按编号依次存放各件货物的重量，D 存放天数，代码略
ans = max(weights)      # ans 存放列表 weights 的最大值
while not canShip(weights, D, ans):
    ans = ans + 1
print(ans)              # 输出最小载重
```

① 若 `weights` 为 `[5,6,6,7,8,4,3]`，`D` 为 5，则该车的最小载重为____（填数字）。

② 将函数 `canShip` 改写如下，要实现相同功能，请在补充划线处代码。

```python
def canShip(weights, D, C):
    days = 1
    cur = C
    for w in weights:
        if cur >= w:
            cur -= w
        else:
            days += 1
            if days > D:
                return False
            ____②____
    return True
```

(3) 公司要一天内完成多件货物的运输任务，任务列表 `tasks` 的每个元素为 `[sno, num, start, end]`，表示某件货物编号 `sno`、重量 `num`、装货点编号 `start` 和卸货点编号 `end`。求无人车的最小载重量。请在划线处填入合适的代码。

```python
def insertInSorted(a, event):
    i, j = 0, len(a)-1
    ____①____
    while i <= j:
        m = (i + j) // 2
        if a[m][0] < x:
            i = m + 1
        elif a[m][0] == x and a[m][1] <= event[1]:
            i = m + 1
        else:
            j = m - 1
    # 在列表 a 中下标为 i 的位置插入元素 event；若 i>=len(a)，则在 a 末尾添加元素 event
    a.insert(i, event)
    return a

def assign(tasks):
    a = []
    for t in tasks:
        a = insertInSorted(a, [t[2], t[1]])
        ____②____
    return a

def proc(a):
    cur = 0
    maxw = 0
    for data in a:
        ____③____
        if cur > maxw:
            maxw = cur
    return maxw

# tasks 列表存放多件货物运输任务
a = assign(tasks)
capacity = proc(a)
print("无人车的最小载重量为:", capacity)
```

---

## 答案

(1) 12  
(2) ① 11；② `cur = C - w`  
(3) ① `x = event[0]`；② `a = insertInSorted(a, [t[3], -t[1]])`；③ `cur += data[1]`

---

## 解析

(1) 按装卸点扫描同车一天运输任务，在各区间累计车上货物重量，最大同时载重为 12。  
(2) `canShip` 判断容量 `C` 能否在 `D` 天内按顺序完成运输，`weights=[5,6,6,7,8,4,3]` 时最小可行容量为 11。改写版中 `cur` 表示当天剩余容量，开启新一天并装入当前货物后剩余容量应为 `C - w`。  
(3) 每件货物在装货点产生正重量事件，在卸货点产生负重量事件；事件按地点排序，同地点先处理卸货（负数）再处理装货（正数）。逐事件累计 `cur`，最大值就是所需最小载重量。

---

## 相关链接

- 类似知识点题：[[09队列/202406-宁波九校-高二-期末-15]]、[[02算法/202604-杭州-高三-二模-15]]、[[13查找与排序/202506-宁波九校-高二-期末-12]]、[[13查找与排序/202512-Z20名校联盟-高三-月考-12]]、[[13查找与排序/202512-嘉兴-高三-一模-12]]
- 相关知识点：
