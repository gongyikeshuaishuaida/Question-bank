---
id: 202506-舟山-高二-期末-15
题型: 填空题
来源: "[[试卷/202506-舟山-高二-期末.pdf|202506-舟山-高二-期末.pdf]]"
试卷: 202506-舟山-高二-期末.pdf
年级: 高二
题号: "15"
难度: 困难
水平: 选考
知识点:
  - 08数组
  - 队列模拟
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-04
图片核验: 待核验
tags:
  - 知识点/08数组
---

# 题目 15

某海鲜加工厂有若干种生鲜货物存储在冷库中，冷库中有 n 个超低温速冻柜（编号为 0 到 n-1），每个速冻柜存放 1 件货物。入库出库操作数据存放在记事本文件 `data.txt` 中，如第 15 题图 a 所示，每一行包含 3 个数据，分别是货物名称、操作类型和货物数量。在执行入库和出库操作时，遵循以下几条规则：

① 考虑到货物的保质期，总是从最早入库的货物开始按序出库。
② 考虑到速冻柜在完成出库工作后需要进行清理，最新腾出的空柜总是最后安排入库。
③ 在入库时，若遇到速冻柜都已存满但还有货物需要入库，则把需要入库的货物暂存在等候区，一旦有速冻柜空出优先将等候区的货物入库。

![[attachments/202506舟山_15_图a.jpg]]

![[attachments/202506舟山_15_图b.jpg]]

(1) 在当前状态下新增两条操作数据：`"e, in, 3"`, `"c, out, 3"`，执行完所有入库出库操作后，空闲速冻柜的号码是 ▲ 。

(2) 定义 `insert_storage(name, length)` 函数，函数的功能是实现入库操作。请在划线处填入合适的代码。

```python
def insert_storage(name, length):
    current = head["empty"][0]
    prev = -1
    count = 0
    while current != -1 and count < length:
        ①
        prev = current
        current = cabinet_node[current]
    if name not in head:
        head[name] = [head["empty"][0], prev]
    else:
        cabinet_node[head[name][1]] = head["empty"][0]
        head[name][1] = prev
    cabinet_node[prev] = -1
    head["empty"][0] = ②
    if count < length:
        waitdata.append([name, "in", length - count])
        del head["empty"]
```

(3) 主程序如下，请在划线处填入合适的代码。

```python
with open("data.txt", encoding="utf-8") as f:
    data = [line.strip().split(",") for line in f if line.strip()]
n = int(input("输入速冻柜的个数："))

cabinet_node = list(range(1, n)) + [-1]
cabinet_name = [""] * n
head = {"empty": [0, n - 1]}
waitdata = []
i = j = 0
while i < len(data) or ("empty" in head and j < len(waitdata)):
    if ③:
        insert_storage(waitdata[j][0], int(waitdata[j][2]))
        j += 1
    elif "empty" in head and data[i][1] == "in":
        insert_storage(data[i][0], int(data[i][2]))
        i += 1
    elif data[i][1] == "out":
        pop_storage(data[i][0], int(data[i][2]))
        i += 1
    else:
        waitdata.append(data[i])
        i += 1

# 更新 cabinet_name
for key in head:
    if key == "empty":
        continue
    current = head[key][0]
    while current != -1:
        ④
        current = cabinet_node[current]
print("当前速冻柜的状态是：", cabinet_name)
```

---

## 答案

**正确答案：**

(1) 8

(2) ① `count += 1`；② `current`

(3) ③ `"empty" in head and j < len(waitdata)`；④ `cabinet_name[current] = key`

---

## 解析

(1) 模拟入库出库操作后，空闲柜号码为 8。(2) ① 计数已安排入库的货物。② 更新空闲柜链表头指针。(3) ③ 优先处理等候区货物。④ 遍历链表填充速冻柜状态数组。

---

## 相关链接

- 相关知识点：
