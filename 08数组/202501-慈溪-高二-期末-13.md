---
id: 202501-慈溪-高二-期末-13
题型: 填空题
来源: "[[试卷/202501-慈溪-高二-期末.pdf|202501-慈溪-高二-期末.pdf]]"
试卷: 202501-慈溪-高二-期末.pdf
年级: 高二
题号: "13"
难度: 中等
水平: 学考
知识点:
  - 08数组
  - 字符计数
  - 去重
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-31
图片核验: 无图片
tags:
  - 知识点/08数组
  - 知识点/字符计数
  - 知识点/去重
---

# 题目 13

小明为了记忆单词，设计了一种单词分类方法：两个单词可以分为一类，当且仅当组成这两个单词的各个字母的数量均相等。例如单词 `"eat"` 和 `"tea"` 可以归为一类，但和 `"ear"` 不是一类。所有单词均由小写字母组成。现要统计需要记忆的单词可以被分成几类。

（1）假设小明要记忆的单词为 `["able","ably","bale","last"]`，则可以将这些单词分成______类。

（2）所有需要记忆的单词存储在文本文件“单词.txt”中，每行一个单词。实现上述功能的部分 Python 程序如下，请在划线处填入合适的代码。

```python
def types(s):
    d = [0] * 26
    for t in s:                     # 统计每个字母出现的次数
        ①
    s1 = ""
    # 按字母表顺序重新拼接，例如“good”变为“dgoo”
    for i in range(len(d)):
        if d[i] > 0:
            s1 += ②
    return s1

f = open("单词.txt", "r")
lst = []
for line in f.readlines():
    s = line.strip()                # 去掉单词末尾的“\n”
    if ③:
        continue                    # 此处是方框处
    lst.append(types(s))
print("一共有" + str(len(lst)) + "类单词")
```

（3）若将加框处代码语句修改成 `break`，则对程序运行结果______影响。（单选，填字母：A. 有 / B. 无）

---

## 答案

（1）3

（2）① `d[ord(t)-ord("a")] += 1`；② `chr(i+ord("a")) * d[i]`；③ `types(s) in lst`

（3）A

---

## 解析

（1）`able` 与 `bale` 属于同一类，`ably`、`last` 各自一类，共 3 类。

（2）用长度为 26 的数组统计各字母次数，再按字母顺序生成统一的类别特征串；若特征已出现则跳过。

（3）`continue` 只跳过当前重复单词，`break` 会提前终止整个文件的后续处理，可能漏掉新的类别，因此有影响。

---

## 相关链接

- 来源：[[试卷/202501-慈溪-高二-期末.pdf|202501-慈溪-高二-期末.pdf]]
