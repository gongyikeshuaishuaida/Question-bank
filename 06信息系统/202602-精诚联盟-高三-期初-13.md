---
id: 202602-精诚联盟-高三-期初-13
题型: 填空题
来源: "[[试卷/202602-精诚联盟-高三-期初.pdf|202602-精诚联盟-高三-期初.pdf]]"
试卷: 202602-精诚联盟-高三-期初.pdf
年级: 高三
题号: "13"
难度: 中等
水平: 选考
知识点:
  - 06信息系统
  - 传感器
  - 执行器
  - 分支结构
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-29
图片核验: 无图片
tags:
  - 知识点/06信息系统
  - 知识点/传感器
  - 知识点/执行器
  - 知识点/分支结构
---

# 题目 13

小明模拟搭建智慧图书馆座位预定系统。智能终端连接安装于每个座位上的红外传感器和座位指示灯，红外传感器探测人体是否就座，探测结果控制指示灯的颜色（座位空闲显示绿色，否则显示橙色），并通过无线通信方式向服务器发送座位的状态信息，服务器统计当前座位状态，用户可以通过浏览器查询座位使用情况并预定座位。请回答下列问题：

（1）在搭建该监测系统时，每个智能终端连接的传感器和执行器数量合理的是 ________（单选，填字母：A. 只能连接 1 个传感器和 1 个执行器 / B. 可以连接多个传感器和多个执行器）。

（2）智能终端通过 URL 向服务器传输数据时，需要知道 ________（单选，填字母）。

- A. 智能终端的 IP 地址
- B. 智能终端的 CPU 型号
- C. 服务器中对应路由名称
- D. 服务器中数据库名称

（3）现需增加图书馆内空气温湿度监测功能，在智能终端接入温湿度传感器后，必须要对软件部分做的修改有 ________（多选，填字母）。

- A. 增加智能终端程序中向服务器上传温湿度传感器数据的代码
- B. 修改服务器端程序，增加接收温湿度传感器数据并写入数据库的代码
- C. 增加接收温湿度传感器数据的新的路由与视图函数的代码
- D. 修改智能终端程序中服务器地址及端口号

（注：全部选对的得 2 分，选对但不全的得 1 分，不选或有选错的得 0 分。）

（4）若用户未能在规定时间内到达，则系统会扣除信用分 `t`（每位用户原始信用分为 600），每超时一次扣 100，实现该功能的代码为 `t=max(0,t-100)`，下列程序段中与其作用相同的是 ________（多选，填字母）。

- A.

  ```python
  t -= 100
  if t < 0:
      t = 0
  ```

- B.

  ```python
  if t < 0:
      t = 0
  else:
      t -= 100
  ```

- C.

  ```python
  if t > 100:
      t -= 100
  if t < 0:
      t = 0
  ```

- D.

  ```python
  if t > 100:
      t -= 100
  else:
      t = 0
  ```

（5）系统运行后，发现某座位没有人坐但是指示灯长期保持橙色。若指示灯本身无故障，且其与智能终端的连接也无故障，请用文字描述可能的原因。（注：回答 2 个可能原因，1 项正确得 1 分。）

---

## 答案

（1）B

（2）C

（3）A、B

（4）A、D

（5）传感器故障；软件逻辑错误（或阈值设置错误等其他合理原因）。

---

## 解析

（1）智能终端可以根据接口和处理能力连接多个传感器及多个执行器。

（2）URL 中需要包含服务器地址、端口和对应路由等信息；四个选项中应知道服务器中用于接收数据的对应路由名称。

（3）增加温湿度采集后，智能终端需要增加上传温湿度数据的代码，服务器端需要增加接收数据并写入数据库的处理。原服务器地址和端口不必修改，也不一定需要另建全新的路由与视图函数。

（4）`max(0,t-100)` 表示扣 100 后若小于 0 则取 0。A 先扣分再判断下限；D 在 `t>100` 时扣 100，否则直接置 0，二者在题设信用分范围内作用相同。B 在 `t=0` 时会得到 `-100`，C 在 `t=100` 时不会扣为 0。

（5）排除指示灯及其连线故障后，可能是红外传感器错误地持续返回“有人”状态，也可能是终端控制程序的判断逻辑或阈值设置错误。

---

## 相关链接

- 相关知识点：
