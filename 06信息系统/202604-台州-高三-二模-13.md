---
id: 202604-台州-高三-二模-13
题型: 填空题
来源: "[[试卷/202604-台州-高三-二模.pdf|202604-台州-高三-二模.pdf]]"
试卷: 202604-台州-高三-二模.pdf
年级: 高三
题号: "13"
难度: 困难
知识点:
  - 06信息系统
  - 物联网系统
  - 数据传输
  - 03python基础
完成次数: 0
正确率:
状态: 未练习
错题原因:
创建日期: 2026-06-23
图片核验: 无图片
tags:
  - 状态/未练习
  - 知识点/06信息系统
  - 知识点/物联网系统
  - 知识点/数据传输
---

# 题目 13

13. 某校学生宿舍安装智能用电监控系统，每个宿舍放置一个智能终端，电流传感器监测宿舍用电功率，每 60 秒采集一次数据，并由智能终端通过无线通信方式将数据传输至服务器，存储在数据库中。服务器实时处理数据，当监测到某个宿舍用电功率超过阈值则向管理员发送警报，并可智能终端发送控制指令，由继电器模块控制电源通断。

(1) 关于该系统中硬件连接的说法，正确的是____(单选)。

A. 一个智能终端只能连接一个电流传感器  
B. 一个智能终端可以同时连接电流传感器和继电器模块

(2) 关于该系统数据传输与存储的说法，正确的有____(单选)。

A. 智能终端只能通过 Wi-Fi 方式将数据上传至服务器  
B. 传感器采集的数据先经过智能终端，再上传到服务器  
C. 管理员接收到的警报信息是由智能终端发送的

(3) 为了降低该系统数据传输的网络负荷，下列做法可行的是____(多选)。

A. 设置传感器每 30 秒采集一次数据  
B. 增加宿舍的电流传感器数量  
C. 智能终端将与之连接的多个传感器所采集数据统一打包压缩后再上传至服务器  
D. 智能终端对传感器采集数据进行判断，若与上一次数据不同则上传至服务器

(4) 智能终端上的程序具有如下功能：当网络异常时，将数据暂存到本地（缓存上限为 500 条）；当网络恢复正常时，先上传缓存数据，再上传当前数据。部分 Python 程序如下，请在程序中划线处填入合适的代码。

```python
buffer = []  # 缓存数据列表
while True:
    power = read_power()  # 采集数据
    if network_ok():  # 网络正常
        for p in buffer:
            upload(p)  # 上传缓存数据
        # 清除缓存数据，代码略
        ____①____
    else:  # 网络异常
        if ____②____:
            # 删除缓存列表中最早存入的数据元素，代码略
        # 将当前数据 power 添加到缓存数据列表 buffer 的末尾，代码略
    # 延时 60 秒，代码略
```

(5) 系统运行时，管理员频繁收到某个宿舍用电功率超过阈值的警报，经核查，该宿舍并未使用大功率电器。简要说明可能造成上述问题的原因。（注：回答 2 项，1 项正确得 1 分）

---

## 答案

(1) B

(2) B

(3) C、D

(4) ① `upload(power)`；② `len(buffer) == 500` 或 `len(buffer) > 500`

(5) ① 电流传感器故障，读数偏大；② 智能终端发生故障，导致误判；③ 电网电压波动导致功率瞬时超标。任写两条即可。

---

## 解析

智能终端可以连接采集设备和控制设备；传感器数据需先到智能终端再上传服务器。打包压缩和差异上传都能减少传输量。网络正常时当前数据仍需上传；网络异常且缓存达到上限时，要先删除最早数据再追加当前数据。误报警可能来自传感器、智能终端或外部电网波动。

---

## 相关链接

- 来源：[[试卷/202604-台州-高三-二模.pdf|202604-台州-高三-二模.pdf]]
