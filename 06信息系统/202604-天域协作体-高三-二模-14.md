---
id: 202604-天域协作体-高三-二模-14
题型: 填空题
来源: "[[试卷/202604-天域协作体-高三-二模.pdf|202604-天域协作体-高三-二模.pdf]]"
试卷: 202604-天域协作体-高三-二模.pdf
年级: 高三
题号: "14"
难度: 中等
水平: 选考
知识点:
  - 06信息系统
  - 04数据处理
  - Flask
  - pandas
  - 系统故障
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-30
图片核验: 已通过
tags:
  - 知识点/06信息系统
  - 知识点/04数据处理
  - 知识点/Flask
  - 知识点/pandas
  - 知识点/系统故障
---

# 题目 14

某企业在多个生产车间搭建环境监测系统，各车间配置一个智能终端，智能终端连接温度传感器和风扇，智能终端每小时采集一次温度数据，智能终端通过同一个 Wi-Fi 将温度数据传输至服务器，存储到数据库。服务器根据数据判断出异常情况时，通过智能终端控制风扇运行。用户可通过浏览器查看系统数据。请回答下列问题：

（1）下列关于搭建该信息系统的说法，正确的是 ________（单选）。

- A. 可用 Flask 开发智能终端程序
- B. 可用 SQLite3 管理数据库
- C. 既要编写服务器端程序，也要编写客户端程序

（2）下列关于该系统中数据的说法，正确的是 ________（多选）。

- A. 智能终端有采集功能，无处理与存储功能
- B. 智能终端与执行器、服务器之间数据双向传输
- C. 用户查看历史数据需要访问数据库
- D. 浏览器传输数据到服务器需要遵循 HTTP 协议

（3）某智能终端将采集到的温度值传至服务器的 URL 为 `HTTP://10.101.11.165:8080/tran?id=A&t=32`，则服务器相应的路由是 `@app.route("________")`。

（4）系统正常工作一段时间后，企业用户查询不到 A 车间的实时温度数据，请分析可能的原因。（本系统中，传感器损坏、传感器和智能终端连接异常，不会造成上述问题）

（5）将当年 6—9 月份的数据导出到文件 `data.xlsx` 中。部分数据如第 14 题图 a 所示。统计各车间温度超过 $30^\circ\mathrm{C}$ 的次数，超过 $30^\circ\mathrm{C}$ 次数最多的车间各月份的平均温度，绘制第 14 题图 b 所示的柱形图。

![[attachments/202604天域协作体高三_14_图1.png]]

![[attachments/202604天域协作体高三_14_图2.jpg]]

实现上述功能的部分 Python 程序如下，请选择合适的代码填入划线处（单选）。

```python
import pandas as pd
import matplotlib.pyplot as plt

df = pd.read_excel("data.xlsx")
____①____
df1 = df1.groupby("车间", as_index=False).count()  # 分组计数
____②____
c = df1.values[0][0]  # 温度超过30℃次数最多的车间
df2 = df[df["车间"] == c]  # 筛选
for i in df2.index:
    ____③____
df2 = df2.groupby("月").mean()  # 分组求平均值
plt.bar(____④____)
plt.title(c + "车间6~9月月温度平均值")
# 设置绘图参数，代码略
```

程序中①②③④处可选的代码有：

- A. `df1=df["温度"]>30`
- B. `df1=df[df["温度"]>30]`
- C. `df1=df.sort_values("车间", ascending=False)`
- D. `df1=df1.sort_values("日期", ascending=False)`
- E. `df1=df1.sort_values("温度")`
- F. `df2["月"]=df2["日期"][5:7]`
- G. `df2.at[i,"月"]=df2.at[i,"日期"][5:7]`
- H. `df2.index,df2["温度"]`
- I. `df2["月"],df2["温度"]`
- J. `df2.index,df2.values`

---

## 答案

（1）B

（2）C、D

（3）`/tran`

（4）示例：A 车间的智能终端故障，或 A 车间物联网模块故障

（5）① B；② D；③ G；④ H

---

## 解析

（1）SQLite3 可用于管理数据库；Flask 用于开发服务器端 Web 应用，不是智能终端程序。

（2）查看历史数据需要访问数据库，浏览器与服务器通信需遵循 HTTP 协议。

（3）URL 的路径部分是 `/tran`。

（4）在排除传感器及其连接故障后，智能终端或物联网通信模块故障会导致实时数据无法上传。

（5）先筛选温度大于 30 的记录，再按车间计数；按计数所在的“日期”列降序排列后取首行车间。逐行从日期字符串提取月份，按月分组求平均；分组后的月份位于索引中，柱形图使用 `df2.index` 和 `df2["温度"]`。

---

## 相关链接

- 相关知识点：
