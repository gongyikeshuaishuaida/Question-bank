---
id: 202506-舟山-高二-期末-14
题型: 填空题
来源: "[[试卷/202506-舟山-高二-期末.pdf|202506-舟山-高二-期末.pdf]]"
试卷: 202506-舟山-高二-期末.pdf
年级: 高二
题号: "14"
难度: 中等
水平: 选考
知识点:
  - 06信息系统
  - 门禁系统与数据处理
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-04
图片核验: 已通过
tags:
  - 知识点/06信息系统
---

# 题目 14

某校引进了校园安全管理系统，该系统具备门禁管理功能。学校预先将学籍管理系统内的照片数据导入至安全系统的图像数据库中。通过在校门口安装的 AI 摄像头，系统能自动捕捉进出人员的面部特征，并与数据库中的图像数据进行匹配。检测到身份核验通过的校内人员时，闸机自动放行。此外，该系统能实时记录学生的到校和离校时间，并据此生成考勤报告。

![[attachments/202506舟山_14_图a.jpg]]

(1) 根据该系统的功能要求，图中 ①、② 处的设备名称分别是 ▲ （单选，填字母：A. AI 摄像头、闸机 / B. 闸机、AI 摄像头）。

(2) 下列有关该系统的说法，正确的是 ▲ （多选，填字母）。
- A. 该系统的架构为 C/S 模式
- B. 为保证数据资源的安全，需要为班主任、行政教师、科任教师等设置不同的访问权限
- C. 选择数据管理系统属于前期准备中的详细设计
- D. 搭建完该系统后，需要进行软件测试、硬件测试、网络测试等

(3) 该系统服务器的 IP 地址为 `192.168.10.16`，端口号为 `8000`，若要查看 AI 摄像头采集的数据，应访问的页面地址是 `http://` ▲ 。

(4) 在系统测试阶段，学校发现存在部分特定学生无法识别的现象，造成这种现象的原因不可能是 ▲ （单选，填字母）。
- A. 预先导入的图像数据分辨率较低
- B. 识别算法的阈值设置过高
- C. 背景人流密集导致误检
- D. IOT 模块停止工作

(5) 为排查故障原因，管理人员将 3 月份第一周的考勤异常数据导出为 `"异常名单.xlsx"` 文件，如图 b 所示，并统计各班"未识别"的学生数量制作成图表，如图 c 所示，部分程序如下。

![[attachments/202506舟山_14_图b.jpg]]

![[attachments/202506舟山_14_图c.jpg]]

```python
import pandas as pd
import matplotlib.pyplot as plt
plt.rcParams["font.sans-serif"] = "SimHei"

df = pd.read_excel("异常名单.xlsx")
df1 = ①  # 筛选"未识别"的记录
df2 = df1.groupby("班级", as_index=True).count()
df2 = df2.rename(columns={"学号": "未识别人数"})
df3 = df2.sort_values("未识别人数", ascending=False)
x = ②
y = df3["未识别人数"]
plt.bar(x, y)
plt.title("三月第1周各班未识别人数")
plt.show()
```

① 加框处代码应为 ▲ （单选，填字母：C. `df[df["考勤状态"] == "未识别"]`）。
② 请在划线处填入合适的代码 ▲ 。

---

## 答案

**正确答案：**

(1) A　　(2) B、D　　(3) `192.168.10.16:8000/view`　　(4) D

(5) ① C（`df[df["考勤状态"] == "未识别"]`）；② `df3.index`

---

## 解析

(1) 摄像头捕获人脸后闸机放行，顺序为 AI 摄像头 → 闸机。(2) B 权限分级和 D 全面测试正确。(3) URL 格式为 IP:端口/路径。(4) IOT 模块故障只影响数据传输，不影响识别本身。(5) ① 筛选考勤状态为"未识别"的行。② x 轴使用班级名称（索引）。

---

## 相关链接

- 相关知识点：
