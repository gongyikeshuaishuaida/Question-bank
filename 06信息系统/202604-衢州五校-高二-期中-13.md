---
id: 202604-衢州五校-高二-期中-13
题型: 填空题
来源: "[[试卷/202604-衢州五校-高二-期中.pdf|202604-衢州五校-高二-期中.pdf]]"
试卷: 202604-衢州五校-高二-期中.pdf
年级: 高二
题号: "13"
难度: 困难
水平: 选考
知识点:
  - 06信息系统
  - 04数据处理
  - pandas
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-30
图片核验: 已通过
tags:
  - 知识点/06信息系统
  - 知识点/04数据处理
  - 知识点/pandas
---

# 题目 13

某智慧物流园区部署了自动导引车（AGV）调度系统。该系统通过部署在园区内的传感器和 AGV 上的智能终端，实时采集车辆位置、电量及货物信息，并上传至云端服务器进行处理。当检测到路径拥堵或电量不足时，服务器会下发指令调整任务。请回答下列问题。

（1）为了高效准确地识别园区内的货物信息，以下哪种标签更合理？（单选，填字母）

- A. RFID 电子标签
- B. 二维码

（2）研究小组通过浏览器访问 AGV 的实时运行轨迹，处理该浏览器访问请求的设备是（单选，填字母）

- A. 传感器
- B. 服务器
- C. 智能终端
- D. 执行器

（3）智能终端是 AGV 的核心控制部件，下列关于智能终端程序需要实现的功能，说法正确的有（多选，填字母）

- A. 实时采集激光雷达、编码器等传感器的数据
- B. 根据全园区的订单分布情况，计算全局最优路径
- C. 接收服务器下发的运动指令，将其转换为电机驱动信号以控制车轮转动
- D. 监测电池电量，当电量低于阈值时向服务器发送充电请求

（4）随着园区业务量的激增，在处理海量 AGV 运行轨迹数据时，服务器响应较慢，在不考虑更换服务器的前提下，可以采取哪些措施来提升服务器的性能？（注：回答两点，每点正确得 1 分，答案多于两点只看前两点）

（5）智慧物流园区对 A、B、C、D 四个作业分区的订单处理数据进行了为期一个月的采集，部分数据 `data.xlsx` 如第 13 题图 1 所示。并统计这四个分区近一个月的平均订单处理量，绘制垂直柱形图（如第 13 题图 2 所示），以直观对比各分区的作业负荷情况。

![[attachments/202604衢州五校高二_13_图1.jpg]]

![[attachments/202604衢州五校高二_13_图2.jpg]]

实现上述功能的 Python 程序如下，请将合适的代码填入划线处。

```python
import pandas as pd
import matplotlib.pyplot as plt

df = pd.read_excel("data.xlsx")
# 数据清洗：为了统计正常作业能力，需要先剔除“异常标记”为 1 的批次数据
df_clean = ________①________
grouped = df_clean.groupby(["分区"], as_index=False)["订单量"].mean()
plt.bar(________②________)
# 设置绘图参数，并显示如第 13 题图 2 所示的垂直柱形图，代码略
```

---

## 答案

（1）A

（2）B

（3）A、C、D

（4）示例：更换性能更强的 CPU；增大服务器内存。

（5）

① `df[df["异常标记"] == 0]`

② `grouped["分区"], grouped["订单量"]`

---

## 解析

（1）RFID 电子标签无需保持可视和精确对准，能够快速、批量识别货物，更适合物流园区。

（2）浏览器发送请求后，由服务器处理并返回实时运行轨迹数据。

（3）智能终端负责本车传感器数据采集、接收控制指令、驱动执行器和监测本车状态；全园区的全局调度更适合由服务器完成，因此选 A、C、D。

（4）可以升级服务器内部硬件或优化资源使用，例如升级 CPU、增加内存、定期清理系统日志释放磁盘空间。

（5）正常批次的“异常标记”为 0，应使用布尔索引筛选；`plt.bar` 的横坐标取分区列，纵坐标取分组计算后的平均订单量列。

---

## 相关链接

- 相关知识点：
