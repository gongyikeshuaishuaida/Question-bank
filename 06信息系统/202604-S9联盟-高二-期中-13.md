---
id: 202604-S9联盟-高二-期中-13
题型: 填空题
来源: "[[试卷/202604-S9联盟-高二-期中.pdf|202604-S9联盟-高二-期中.pdf]]"
试卷: 202604-S9联盟-高二-期中.pdf
年级: 高二
题号: "13"
难度: 中等
水平: 选考
知识点:
  - 06信息系统
  - 04数据处理
  - Flask
  - pandas
  - 系统故障
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-30
图片核验: 已通过
tags:
  - 知识点/06信息系统
  - 知识点/04数据处理
  - 知识点/Flask
  - 知识点/pandas
  - 知识点/系统故障
---

# 题目 13

小明搭建了小鸡自动孵化系统，主要是为孵化箱提供一个合适的温度，模拟母鸡孵小鸡的过程。温度传感器的数据由智能终端经 IoT 模块发送到 Web 服务器，当温度数据异常时（超过一定的温度范围），蜂鸣器会报警；也可以通过浏览器随时查询实时温度数据和历史温度数据，并且通过浏览器控制“温度调节器”调节环境温度。请回答下列问题：

（1）该系统网络应用软件的实现架构是 ________（单选，填字母：A. B/S 架构；B. C/S 架构）。

（2）若提交数据到 Web 服务器的 URL 为 `http://192.168.10.2:8080/input?t=27`，则服务器端与该 URL 关联的路由设置语句是 `@app.route("________")`。

（3）系统正常运行一段时间后，如果 IoT 模块发生故障（假设系统内没有其他故障），下列现象中，可能会出现的是 ________（多选，填字母）。

- A. 无法通过浏览器随时查询实时温度数据
- B. 可以打开历史数据页面，但是缺失某时刻之后的数据
- C. 可以通过浏览器控制“温度调节器”调节环境温度
- D. 某时刻之后，传感器无法采集温度数据
- E. 现场观察到温度数据异常时，蜂鸣器未报警

（注：全部选对的得 2 分，选对但不全的得 1 分，不选或选错的得 0 分）

（4）现需增加湿度监测的功能，在智能终端接入湿度传感器后，还需对软件部分作多处修改。请用文字描述其中 1 处修改建议。

（5）管理员整理出某一天的温度监测值，并存入数据文件 `0517.xlsx`，部分数据如第 13 题图 a 所示，现要求找出温度值超过平均值次数最多的时刻（小时），并将该时刻的温度数据绘制折线图如第 13 题图 b 所示。

![[attachments/202604S9联盟高二_13_图1.png]]

![[attachments/202604S9联盟高二_13_图2.png]]

实现上述功能的部分 Python 程序如下：

```python
import pandas as pd
import matplotlib.pyplot as plt

df = pd.read_excel("0517.xls")
df.insert(0, "小时", "")  # 插入列
for i in df.index:  # 取出时间列中的小时
    t = df.at[i, "时间"]
    df.at[i, "小时"] = t[6:8]
val = df.温度.mean()
df1 = df[df.温度 >= val]
df2 = ____①____
df3 = ____②____
sk = df3.at[0, "小时"]
df4 = ____③____
plt.bar(df4.时间, df4.温度)
# 设置绘图参数，显示如第13题图b所示的线形图，代码略
```

①②③处可选代码有：

- A. `df[df.小时==sk]`
- B. `df3[df3.小时==sk]`
- C. `df2.sort_values("温度", ascending=False, ignore_index=True)`
- D. `df2.sort_values("温度", ascending=True, ignore_index=True)`
- E. `df1.groupby("小时", as_index=False).count()`
- F. `df1.groupby("小时").count()`

---

## 答案

（1）A

（2）`/input`

（3）A、B、E

（4）示例：修改智能终端程序，增加向服务器上传湿度传感器数据的代码；或修改服务器端程序，增加接收湿度传感器数据并写入数据库的代码。

（5）① E；② C；③ A

---

## 解析

（1）用户通过浏览器访问 Web 服务器，属于 B/S 架构。

（2）URL 的路径部分是 `/input`。

（3）IoT 模块故障会中断智能终端与服务器间的数据传输，导致实时数据无法查询、之后的历史数据缺失，依赖通信的报警也可能失效；传感器仍可采集数据，浏览器也无法正常下发控制指令。

（4）新增传感器后，采集、上传、接收和数据库存储等相关程序都需同步支持湿度数据。

（5）先按小时统计温度不低于平均值的记录数，再按计数列“温度”降序排列，取次数最多的小时，最后从原始数据中筛选该小时的全部记录。

---

## 相关链接

- 相关知识点：
