---
id: 202606-台州-高二-期末-13
题型: 填空题
来源: "[[试卷/202606-台州-高二-期末.pdf|202606-台州-高二-期末.pdf]]"
试卷: 202606-台州-高二-期末.pdf
年级: 高二
题号: "13"
难度: 中等
水平: 选考
知识点:
  - 06信息系统
  - 04数据处理
  - 信息系统组成
  - pandas
  - 数据分组
  - 数据可视化
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-28
图片核验: 已通过
tags:
  - 知识点/06信息系统
  - 知识点/04数据处理
  - 知识点/信息系统组成
  - 知识点/pandas
  - 知识点/数据分组
  - 知识点/数据可视化
---

# 题目 13

13. 某小组模拟搭建停车场智能管理系统，采用智能终端连接摄像头，并通过无线通信方式将图像数据传输至服务器，服务器处理后，通过智能终端在显示屏上显示车牌和费用信息并控制道闸的升降。系统支持通过微信公众号推送停车账单，管理员可通过浏览器查看系统数据。请回答下列问题：

(1) 该系统中负责车辆信息采集的设备是 ______（单选）。

- A. 摄像头
- B. 道闸
- C. 显示屏

(2) 该系统智能终端与其他设备之间的数据传输关系，合理的是 ______（单选）。

- A. 只能由服务器传输至智能终端
- B. 只能由智能终端传输至显示屏
- C. 只能由智能终端传输至摄像头

(3) 关于该系统功能的说法，正确的有 ______（多选）。（注：全部选对的得 2 分，选对但不全的得 1 分，不选或有选错的得 0 分）

- A. 服务器可直接控制入口道闸的升降动作
- B. 计算停车费用的功能可在智能终端完成，以减轻服务器压力
- C. 微信公众号推送停车账单的功能可由服务器端实现
- D. 服务器发生故障时，管理员仍可通过浏览器查看历史数据

(4) 系统测试时发现车辆驶入时显示屏上能正常显示车牌信息，但入口道闸始终不抬杆，请分析并写出两种可能的原因。

(5) 将该系统某月的车辆出场记录导出到文件 `parking.xlsx` 中，部分数据如第 13 题图 a 所示。统计该月每天的收费总额，选择总额最多的前 5 天数据，绘制如第 13 题图 b 所示的柱形图。

![[attachments/202606台州高二_13_图1.png]]

实现上述功能的部分 Python 程序如下，请选择合适的代码填入划线处（单选）。

```python
import pandas as pd
import matplotlib.pyplot as plt
df = pd.read_excel("parking.xlsx")
df1 = ______ ①
df1 = ______ ②
df1 = ______ ③

# 设置绘图参数，选取 df1 中的数据创建图表，代码略
```

程序中①②③处可选的代码有：

- A. `df[df[是否出场]=="已出场"]`
- B. `df[df.是否出场=="已出场"]`
- C. `df1.sort_values("停车费用",ascending=True).tail(5)`  # 升序排序
- D. `df1.sort_values("停车费用",ascending=False).head(5)`
- E. `df1.groupby("日",as_index=False).停车费用.count()`  # 分组计数
- F. `df1.groupby("日",as_index=False).停车费用.sum()`

---

## 答案

**正确答案：** (1) A；(2) B；(3) B、C；(4) 示例：道闸（执行器）本身损坏；智能终端与道闸之间的连接线路故障；(5) ① B；② F；③ D

---

## 解析

(1) 摄像头用于采集车辆图像，属于输入设备。

(2) 显示屏接收智能终端输出的信息，数据方向为智能终端到显示屏；服务器与终端、摄像头与终端之间还存在相反方向的数据传输。

(3) 费用可在智能终端计算，推送停车账单可由服务器端实现。服务器不能绕过终端直接控制道闸；服务器故障时，浏览器通常无法查询其保存的历史数据。

(4) 显示屏正常说明图像采集和车牌识别链路基本正常，故障可能位于道闸本体、终端与道闸的连接线路、终端控制程序或服务器下发控制指令等环节。

(5) 先筛选“已出场”记录，选择 B；再按“日”分组求停车费用总和，选择 F；最后按停车费用降序取前 5 条，选择 D。

---

## 相关链接

- 相关知识点：
