---
id: 202503-天域协作体-高三-二模-14
题型: 非选择题
来源: "[[试卷/202503-天域协作体-高三-二模.pdf|202503-天域协作体-高三-二模.pdf]]"
试卷: 202503-天域协作体-高三-二模.pdf
年级: 高三
题号: "14"
难度: 中等
水平: 选考
知识点: [04数据处理, pandas, 分组统计, 数据可视化]
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-08-01
图片核验: 已通过
tags: [知识点/04数据处理]
---
# 题目 14

小明制作了可以自动加热加水的智能饮水机系统。温度传感器和水位传感器采集得到的数据由智能终端经 IOT 模块发送到 Web 服务器。若水温低于系统设置的阈值，则开启加热器；若水位低于系统设置的阈值，则打开阀门加水。小明可以通过浏览器查看实时监测数据和历史数据。请回答下列问题。

（1）该系统中的温度传感器和水位传感器______（单选，填字母：A.一定 / B.不一定）要连接在同一智能终端。

（2）若温度传感器的编号 `id` 为 1，温度值 `t` 为 20，提交数据到 Web 服务器的 URL 为 `http://192.168.10.6:8080/temp?id=1&t=20`，则服务器的 IP 地址为______。

（3）关于该饮水机系统，下列说法合理的是______（多选，填字母）。

- A. 智能终端故障会影响小明查看实时数据
- B. 测试并观察该系统运行时的行为，属于静态测试
- C. 该系统中，传感器与智能终端之间的数据传输是单向的
- D. 温度传感器和水位传感器必须同时传送数据到智能终端

（4）系统运行一段时间后，小明发现饮水机的加水功能正常，但无法正常加热，简要说明系统中可能造成上述问题的原因（回答 2 项）。

（5）水位传感器每隔一分钟检测一次，若水位低于 20 cm，打开阀门加水一分钟，加水过程中会关闭出水口。为了解家庭饮水情况，小明将系统中 9 月份的水位数据导出，部分数据如图 a 所示。现利用 Python 编写程序统计 9 月份的每日饮水量，绘制如图 b 所示图表。

![[attachments/202503天域协作体高三_14_图1.png]]

![[attachments/202503天域协作体高三_14_图2.jpg]]

部分 Python 程序如下：

```python
import pandas as pd
import matplotlib.pyplot as plt
df=pd.read_excel("data.xlsx")
df["日期"]=[""]*len(df)
df["水量变化"]=[0]*len(df)
for i in range(len(df)):
    df["日期"][i]=df["时间"][i][8:10]
for i in range(1,len(df)):
    df["水量变化"][i]=df["数据"][i-1]-df["数据"][i]
df1=df[____①____]
# 对水量变化值进行筛选，以便后续代码正确统计每日饮水量
____②____  # 此处是方框处
plt.title("9 月份每日饮水量统计图")
plt.show()
```

方框中可以依次填入下列语句（选 3 项，填字母，少选、多选、错选或次序错均不得分）：

- A. `df1=df1.sum()`
- B. `df1=df1.count()`
- C. `df1=df1.groupby("日期")`
- D. `df1=df1.groupby("日期",as_index=False)`
- E. `plt.plot(df1.index,df1.水量变化)`

---
## 答案

**正确答案：**（1）B；（2）`192.168.10.6`；（3）AC；（4）温度传感器故障、传感器与智能终端连接异常、加热器故障等任答两项；（5）① `df["水量变化"]>0`；② `C、A、E`。

---
## 解析

传感器可以分别接入不同智能终端，因此不一定连接在同一终端。URL 中主机部分是 `192.168.10.6`，端口为 `8080`，路径和查询参数不属于 IP。系统运行时观察行为属于动态测试，传感器向终端发送采集数据是单向传输，两个传感器也不必同时发送，因此（3）选 A、C。加水正常而加热异常时，应优先排查温度传感器、通信连接和加热器本身。

水位下降表示饮水，故 `水量变化=前一条数据-当前数据` 为正时保留；按“日期”分组后对每日变化求和，最后以分组索引和汇总值绘图，所以语句顺序为 C、A、E。独立检查图 b 的每日序列与正变化量汇总逻辑一致。

---
## 相关链接

- 相关知识点：DataFrame 条件筛选、`groupby`、分组求和、折线图
