---
id: 202604-浙里特色-高二-期中-14
题型: 填空题
来源: "[[试卷/202604-浙里特色-高二-期中.pdf|202604-浙里特色-高二-期中.pdf]]"
试卷: 202604-浙里特色-高二-期中.pdf
年级: 高二
题号: "14"
难度: 困难
水平: 选考
知识点:
  - 04数据处理
  - pandas
  - 数据清洗
  - 数据可视化
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-30
图片核验: 已通过
tags:
  - 知识点/04数据处理
  - 知识点/pandas
  - 知识点/数据清洗
  - 知识点/数据可视化
---

# 题目 14

小帅收集了 2025 年某超市的销售数据并存储在“超市销售数据.csv”文件中，部分数据如第 14 题图 a 所示。分析某月的销售情况，生成最畅销的十类商品销售金额的柱形图如第 14 题图 b 所示。部分 Python 程序如下：

![[attachments/202604浙里特色高二_14_图1.jpg]]

第 14 题图 a

![[attachments/202604浙里特色高二_14_图2.jpg]]

第 14 题图 b

```python
import pandas as pd
import matplotlib.pyplot as plt

df = pd.read_csv("超市销售数据.csv")
df.insert(0, "月份", "")
for i in df.index:
    data = str(df.at[i, "销售日期"])
    df.at[i, "月份"] = data[4:6]

m = int(input("请输入需查询的月份:"))
if m >= 10:
    month = str(m)
else:
    month = "0" + str(m)

g = ____▲____
g = g.groupby("大类名称", as_index=False)["销售金额"].____①____
g = ____加框处____
x = g["大类名称"]
y = ____②____
plt.bar(x, y)
# 设置绘图参数，显示如第 14 题图 b 所示的柱形图，代码略
```

请回答下列问题：

（1）图中数据出现“20250132”，属于哪类数据问题？（单选，填字母）

- A. 数据缺失
- B. 数据重复
- C. 逻辑错误
- D. 数据异常

（2）为筛选月份数据，▲处应填入的代码是（单选，填字母）

- A. `df[df["月份"] == month]`
- B. `df["月份" == month]`
- C. `df[df["月份"]] == month`
- D. `df["月份"] == month`

（3）请在程序中划线①、②处填入合适的代码。

（4）加框处实现按“销售金额”降序排列后取前 10 项，下列代码中能正确实现的有（多选）

- A. `g.sort_values("销售金额", ascending=False).tail(10)`
- B. `g.sort_values("销售金额", ascending=False).head(10)`
- C. `g.sort_values("销售金额", ascending=True)[:11]`
- D. `g.sort_values("销售金额", ascending=False)[:10]`

---

## 答案

（1）C

（2）A

（3）① `sum()`；② `g["销售金额"]`

（4）B、D

---

## 解析

（1）日期 `20250132` 中的“32日”不符合日历规则，属于数据内容之间不合逻辑，选 C。

（2）`df["月份"] == month` 生成布尔序列，再用 `df[...]` 进行布尔索引，故应写成 `df[df["月份"] == month]`。

（3）按“大类名称”分组后需对每组“销售金额”求和，使用 `sum()`；柱形图纵轴数据应取汇总后的“销售金额”列。

（4）先降序排列，再用 `head(10)` 或切片 `[:10]` 都能取得前 10 行，因此 B、D 正确。A 取得降序结果的最后 10 行；C 使用升序且取了 11 行。

---

## 相关链接

- 相关知识点：
