---
id: 202506-台州-高二-期末-14
题型: 非选择题
来源: "[[试卷/202506-台州-高二-期末.pdf|202506-台州-高二-期末.pdf]]"
试卷: 202506-台州-高二-期末.pdf
年级: 高二
水平: 期末
题号: "14"
难度: 困难
知识点: [04数据处理, pandas, 06信息系统]
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-08-02
图片核验: 已通过
tags: [知识点/04数据处理]
---
# 题目 14

某环保机构开发了一套城市空气质量监测系统，在 6 个工业园区周边各设置 1 个监测站。服务器端程序采用 Flask Web 框架开发。智能终端连接多种传感器，每隔半个小时采集 1 次 PM2.5、PM10 及二氧化硫浓度数据上传至服务器，用户通过浏览器查看实时监测结果和历史数据。若服务器判断出任一数据超过相应阈值，系统通过智能终端触发 LED 屏发布预警信号。

（1）以下措施不能提升系统所采集数据的准确性的是（单选，填字母）：

- A．选择性能更优的智能终端
- B．选择高精度的 PM2.5 传感器
- C．选择更加合适的位置设置监测站

答案：**A**。

（2）下列功能需要在服务器端程序中实现的是（单选，填字母）：

- A．采集 PM2.5 传感器上的数据
- B．处理浏览器访问请求

答案：**B**。

（3）某工业园区提交数据到 Web 服务器的 URL 为 `http://10.106.164.107:8000/upload?idy=1&idc=1&p=28`，则服务器端应用实例 `app` 中与该 URL 关联的路由设置语句是：

```python
@app.route("/upload")
```

（4）测试系统时，通过浏览器查看实时数据，发现数据超过阈值但 LED 屏未发布预警信号，简要说明可能造成上述问题的原因（回答 2 项）。

可答：LED 屏损坏；LED 屏与智能终端的连接有误；服务器端用于控制 LED 屏的代码有误；或其他等价原因。

（5）将某工业园区一年的数据导出到文件 `data.xlsx` 中，部分数据如下，其中编号 1～3 分别对应 PM2.5、PM10 及二氧化硫传感器。

| 时间 | 编号 | 数值 |
|---|---:|---:|
| 2023-01-01 00:00:00 | 1 | 53 |
| 2023-01-01 00:00:00 | 2 | 90 |
| 2023-01-01 00:00:00 | 3 | 40 |
| … | … | … |
| 2023-12-31 23:30:00 | 1 | 56 |
| 2023-12-31 23:30:00 | 2 | 108 |
| 2023-12-31 23:30:00 | 3 | 34 |

要统计该工业园区一年里 PM2.5 平均值最高的 5 个时间点，并绘制如下柱形图：

![[attachments/202506台州高二_14_柱形图.png]]

```python
import pandas as pd
import matplotlib.pyplot as plt

df = pd.read_excel("data.xlsx")
df["时间点"] = ""
for i in df.index:
    t = df.at[i, "时间"]  # ①
    df.at[i, "时间点"] = t[11:16]

# 此处是方框处（原框内依次选择三条语句）
df1 = df[df.编号 == 1]  # A
df1 = df1.groupby("时间点", as_index=False).mean()  # F
df1 = df1.sort_values("数值", ascending=False).head(5)  # C
plt.bar(df1["时间点"], df1["数值"])
# 设置绘图参数，代码略
plt.show()
```

① **`df.at[i, "时间"]`**（或 `df["时间"][i]`）；② 方框依次为 **A、F、C**。

---
## 答案

（1）A；（2）B；（3）`/upload`；（4）LED 屏损坏、连接错误、服务器控制代码错误等任答两项；（5）① `df.at[i,"时间"]`（或 `df["时间"][i]`）；② AFC。

---
## 解析

服务器负责处理浏览器请求，传感器采集属于终端侧功能。URL 路径部分为 `/upload`，查询参数不写入路由路径。数据处理时先筛选编号 1 的 PM2.5 记录，再按时间点求平均并按数值降序取前 5 个时间点。

---
## 相关链接

- [[06信息系统/202506-台州-高二-期末-05]]
- [[08数组/202506-台州-高二-期末-15]]
