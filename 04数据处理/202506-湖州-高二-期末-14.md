---
id: 202506-湖州-高二-期末-14
题型: 非选择题
来源: "[[试卷/202506-湖州-高二-期末.pdf|202506-湖州-高二-期末.pdf]]"
试卷: 202506-湖州-高二-期末.pdf
年级: 高二
水平: 学考
题号: "14"
难度: 困难
知识点: [04数据处理, pandas, 06信息系统]
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-08-02
图片核验: 已通过
tags: [知识点/04数据处理]
---
# 题目 14

某校使用智慧食堂系统实现选餐、支付和消费数据管理。电子标签记录菜品信息，结算台读取标签并计算金额，各窗口数据传输到 Web 服务器并存储于数据库。

（1）读取电子标签使用的技术是____。（A 网络技术 / B 射频识别 / C 人工智能 / D 红外线扫描）

（2）下列关于数据管理的说法，正确的是____。

- A. 数据无法从服务器端传输至结算台
- B. 数据和程序都应存储在结算台中
- C. 查看学生消费历史数据需要访问数据库
- D. 餐品金额的计算只能在服务器端完成

（3）合理的数据安全措施是____（多选）：A 人脸数据加密存储；B 只保存当天消费记录；C 定时自动备份交易日志；D 非营业时段关闭防火墙。

（4）结算台 IP 为 `192.168.10.200`、端口 `5050`，服务器 IP 为 `192.168.10.10`、端口 `8080`；餐厅管理页面为 `/room`。访问该页面的地址是 `http://____`。

（5）从 `data.xlsx` 中筛选 9 月午餐数据，并由高到低输出三个食堂午餐消费总额。图 a 为原始数据表，图 b 为结果。

![[attachments/202506湖州高二_14_数据表和柱形图.png]]

```python
import pandas as pd
import matplotlib.pyplot as plt
df=pd.read_excel("data.xlsx")
df1=①
df1=②
df2=③
print(df2)
```

选项：

- A. `df1.groupby("食堂名称",as_index=False)["消费金额"].sum()`
- B. `df1.groupby("食堂名称",as_index=False)["消费金额"].count()`
- C. `df.sort_values("消费金额",ascending=False)`
- D. `df1.sort_values("消费金额",ascending=False)`
- E. `df[df["消费类型"]=="午餐"]`
- F. `df1[df1.消费类型=="午餐"]`

---
## 答案

（1）B；（2）C；（3）AC；（4）`192.168.10.10:8080/room`；（5）EAD。

---
## 解析

电子标签使用射频识别；消费历史需查询数据库；加密和定时备份是合理措施。先从 `df` 筛选午餐记录，再按食堂分组求和，最后按消费金额降序排列，故填 E、A、D。
---
## 相关链接
- [[03python基础/202506-湖州-高二-期末-15]]
