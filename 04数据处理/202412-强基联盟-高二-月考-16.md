---
id: 202412-强基联盟-高二-月考-16
题型: 填空题
来源: "[[试卷/202412-强基联盟-高二-月考.pdf|202412-强基联盟-高二-月考.pdf]]"
试卷: 202412-强基联盟-高二-月考.pdf
年级: 高二
题号: "16"
难度: 中等
水平: 选考
知识点:
  - 04数据处理
  - pandas
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-07-04
图片核验: 待核验
tags:
  - 知识点/04数据处理
---

# 题目 16

学校对学生寝室进行满意度调查，现采集了各位同学对寝室各个项目的评分数据，存储于 `data.xlsx` 文件中，部分数据截图如图 a 所示。

![[attachments/202412强基联盟_16_图a.jpg]]

![[attachments/202412强基联盟_16_图b.jpg]]

现采用 Python 程序对数据进行处理，请回答以下问题：

(1) 读取 `data.xlsx` 文件，并插入"总分"列，用于统计各项目的总分，请在划线处填写合适的代码。

```python
import pandas as pd
df = pd.read_excel("①")
df["总分"] = df.团结和谐 + df.文化建设 + df.寝室美化 + df.卧谈闲聊
```

(2) 按寝室分组统计男生寝室总分的平均分，请在划线处填写合适的代码。

```python
dfm = df[df.性别 == "男"]  # 筛选男生寝室
dfg = dfm.groupby("寝室", as_index=False).总分.②
```

(3) 绘制男生寝室总分前 3 名的柱形图，如图 b 所示，请在划线处填写合适的代码。

```python
import matplotlib.pyplot as plt
dfs = dfg.③("总分", ascending=False).head(3)
plt.bar(④)
# 设置绘制参数，代码略
plt.show()
```

---

## 答案

**正确答案：**

(1) ① `data.xlsx`

(2) ② `mean()`

(3) ③ `sort_values`；④ `dfs.寝室, dfs.总分`

---

## 解析

① 填写 Excel 文件名 `"data.xlsx"`。

② 分组后统计平均分，使用 `mean()` 聚合函数。

③ 按总分降序排序，使用 `sort_values` 函数。

④ 柱形图需传入横纵坐标数据：寝室名和总分。

---

## 相关链接

- 相关知识点：
