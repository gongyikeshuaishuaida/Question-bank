---
id: 202512-嘉兴-高三-一模-10
题型: 选择题
来源: "[[试卷/202512-嘉兴-高三-一模.pdf|202512-嘉兴-高三-一模.pdf]]"
试卷: 202512-嘉兴-高三-一模.pdf
年级: 高三
题号: "10"
难度: 中等
水平: 选考
知识点:
  - 10栈
  - 栈模拟
完成次数: 1
补课:
正确率: 0.98
错题原因:
创建日期: 2025-12
图片核验: 无图片
tags:
  - 知识点/10栈
---

# 题目 10

**10．** 有如下 Python 程序段：

```python
chs = input("请输入字符串：")
s = []
res = ""
for ch in chs:
    if s != [] and s[-1] == ch:
        s.pop()
    else:
        s.append(ch)
for i in range(len(s)):
    res += s[i]
print(res)
```

程序运行时，若输出为 `car`，则可能的输入是（   ）

- A. `cbbare33e`
- B. `cbeare33b`
- C. `caeer33rb`
- D. `caar33reb`

---

## 答案

**正确答案：** A

---

## 解析

程序逻辑：遍历输入字符串，用列表 `s` 模拟栈。当栈非空且栈顶元素等于当前字符时，弹出（消除相邻相同字符）；否则压入。最后输出栈中剩余字符。

即该程序**消除相邻相同字符对**，与括号匹配类似。

最终输出为 `car`，即栈中剩余为 `['c', 'a', 'r']`。

逐一验证各选项：

**A. `cbbare33e`**：
- c→栈:[c]
- b→栈:[c,b]
- b→b与栈顶b相同→pop→栈:[c]
- a→栈:[c,a]
- r→栈:[c,a,r]
- e→栈:[c,a,r,e]
- 3→栈:[c,a,r,e,3]
- 3→3与栈顶3相同→pop→栈:[c,a,r,e]
- e→e与栈顶e相同→pop→栈:[c,a,r]
- 输出："car" ✓

**B. `cbeare33b`**：最终栈中为 [c,b,e,a,r,b]，输出不是 "car"。

**C. `caeer33rb`**：最终栈中为 [c,a,r,b]，输出 "carb"。

**D. `caar33reb`**：最终栈中为 [c,r,3,r,e,b]，输出 "cr3reb"。

---

## 相关链接

- 类似知识点题：[[10栈/202501-台州-高二-期末-12]]
- 相关知识点：[[10栈]]
