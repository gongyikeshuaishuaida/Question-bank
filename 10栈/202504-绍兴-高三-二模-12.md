---
id: 202504-绍兴-高三-二模-12
题型: 选择题
来源: "[[试卷/202504-绍兴-高三-二模.pdf|202504-绍兴-高三-二模.pdf]]"
试卷: 202504-绍兴-高三-二模.pdf
年级: 高三
题号: "12"
难度: 中等
水平: 选考
知识点:
  - 10栈
  - 单调栈
  - 数组
完成次数: 0
补课:
正确率:
错题原因:
创建日期: 2026-08-01
图片核验: 无图片
tags:
  - 知识点/10栈
---
# 题目 12

12. 给定长度为 `n` 的数组 `ts`，为每个元素 `ts[i]` 在其后寻找首个更大元素 `ts[j]`。若找到，则将两者位置距离存入 `ans[i]`，否则，`ans[i]` 的值为0。例如数组 `ts` 为 `[33,34,35,31,29,32,36,33]`，则数组 `ans` 为 `[1,1,4,2,1,1,0,0]`。实现该功能的程序段如下，方框处应填入的代码是：

```python
ans = [0] * n
s = [0] * n
top = -1
for i in range(n):
    t = ts[i]
    while top >= 0 and t > ts[s[top]]:
        j = s[top]
        top -= 1
        ____  # 此处是方框处
    top += 1
    s[top] = i
```

- A. `ans[i] = j - i`
- B. `ans[i] += 1`
- C. `ans[j] = i - j`
- D. `ans[i - j] += 1`

---
## 答案

**正确答案：** A

---
## 解析

弹出栈顶下标 `j` 时，当前下标 `i` 是它之后遇到的首个更大元素，位置距离按原卷程序记为 `j - i`，因此方框处填 `ans[i] = j - i`。

---
## 相关链接

- 来源：[[试卷/202504-绍兴-高三-二模.pdf]]
