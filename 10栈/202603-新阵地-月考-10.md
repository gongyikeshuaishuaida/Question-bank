---
id: 202603-新阵地-月考-10
题型: 选择题
来源: "[[试卷/202603-新阵地-月考.pdf|202603-新阵地-月考.pdf]]"
试卷: 202603-新阵地-月考.pdf
题号: "10"
难度: 中等
知识点:
  - 10栈
  - 栈模拟
  - 03python基础
完成次数: 0
正确率:
状态: 未练习
错题原因:
创建日期: 2026-05-25
图片核验: 无图片
tags:
  - 状态/未练习
  - 知识点/10栈
---

# 题目 10

有如下 Python 程序段：

```python
from random import randint

nums = [3, 1, 4, 2]
stack = []
while len(nums) > 0:
    if randint(0, 1) == 0:  # randint(a, b)随机生成一个[a, b]范围内的整数
        nums.append(nums.pop(0))
    else:
        if len(stack) > 0 and nums[0] > stack[-1]:
            stack[-1] = nums.pop(0)
        else:
            stack.append(nums.pop(0))
```

运行该程序，输出的 `stack` 列表不可能为（   ）

A. `[4,2]`  
B. `[2,4]`  
C. `[4,1]`  
D. `[1,4]`

---

## 答案

**正确答案：** D

---

## 解析

每次真正处理队首元素时，若它大于栈顶，就替换栈顶；否则入栈。要得到 `[1,4]`，最后一个元素 4 必须在 1 之后入栈，但当栈顶为 1 且队首为 4 时，程序会执行替换 `stack[-1]=4`，不会把 4 追加到 1 后面，因此 `[1,4]` 不可能出现。

---

## 相关链接

- 来源：[[试卷/202603-新阵地-月考.pdf|202603-新阵地-月考.pdf]]
